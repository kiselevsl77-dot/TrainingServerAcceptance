# Пульт испытаний сервера обучения Energomera

Временный (альтернативный) UI для **испытаний и приёмки текущей версии сервера
обучения** по OpenAPI-спецификации `SOM1.json` — без доработки API сервера.
Все действия выполняются прозрачно (каждый запрос виден и протоколируется),
а результат оформляется отчётом об испытаниях.

> Основной UI (`TrainingServerUI`) развивается по своему плану (FR-1…FR-8).
> Пульт — независимый проект: полная изоляция, собственная копия `client/` и
> `lib/`, см. `VENDOR.md`. Наработки пульта, полезные основному UI — `BACKPORT.md`.

## Документация испытаний

* **[`docs/12. Руководство пользователя пульта испытаний (функционал и правила).md`](<docs/12. Руководство пользователя пульта испытаний (функционал и правила).md>)**
  — что делает каждый раздел программы и как им пользоваться: архитектура и 10 диаграмм mermaid
  (компоненты, карта разделов, путь HTTP-запроса, воспроизводимость проверки, конвейер оценки
  результатов, сборка отчёта, жизненный цикл сессии, FSM-1, объединение записей, источники состава
  датасета), по главе на каждый экран, правила безопасной работы, известные дефекты API.
  Разделы, реализованные на этапах **T0–T5**, описаны как есть; разделы **T6–T11** помечены как
  *проектируемые*.
* **[`docs/14. Концепт пульта — целевой бизнес-процесс испытаний.md`](<docs/14. Концепт пульта — целевой бизнес-процесс испытаний.md>)**
  — концепт, по которому развивается пульт: принцип проведения испытаний, целевое
  разделение экранов («планирование / прогон / протокол / архив»), порядок навигации
  (пять групп, первая — «Планирование испытаний») и бизнес-процесс
  приёмки сервера обучения ML-моделей в идеальном варианте (таблицы и 8 диаграмм mermaid).
* **[`docs/15. ТЗ на Пульт испытаний (целевое, по концепции).md`](<docs/15. ТЗ на Пульт испытаний (целевое, по концепции).md>)** —
  **[`docs/16. Макет интерфейса ПУЛЬТА (целевые экраны).md`](<docs/16. Макет интерфейса ПУЛЬТА (целевые экраны).md>)** —
  **[`mockup/`](mockup/README.md)** — целевой комплект «идеального состояния» пульта,
  не зависящий от текущего кода: ТЗ (FR-P / DR-P / IR-P / AR-P / NFR-P / AC-P, этапы M0–M8),
  макет 15 экранов `SCR-101…SCR-501` (включая раздел «Планирование испытаний»: наборы проверок
  и программа сессии) и офлайн-прототип интерфейса
  (откройте `mockup/index.html` в браузере — работает без сети и без сборки).
* `docs/01` — ТЗ, `docs/02` — чек-лист испытаний, `docs/03…docs/13` — отчёты этапов, `docs/README.md`
  — полный перечень документов и версии контракта API.

## Состояние на 22.09.2026: этап 2 big bang

Пульт перестраивается по целевому комплекту (`docs/14–16`, прототип `mockup/`), «хвосты»
старого интерфейса не тянутся:

* **ядро** — библиотека наборов (`sets.py`), программа сессии с объединением по ИЛИ и
  покрытием разделов (`programme.py`), очередь прогона как снимок утверждённой ревизии
  (`queue.py`), единый след результата и история повторов (`results.py`), исполнитель
  «▶ Следующая: `TC-…`» с режимами «сценарием/по вызовам», «Пачка tech» и «Авто с паузой»
  (`runner.py`); схема сессии — **v7**;
* **интерфейс** — навигация из 15 целевых экранов `SCR-101`…`SCR-501` в пяти группах
  (первая — «Планирование испытаний»); сейчас экраны открываются как **скелеты** (назначение,
  блоки, состояния, переходы и требования из `docs/16`), наполнение данными — этап 3;
* **старый UI удалён**: 11 экранов (`ui/pages/**`), панель плана, `plan.py`, `plan_runner.py`
  и поле `plan` схемы v6. Прежняя версия пульта доступна в ветке `stable` (тег `pre-bigbang`).

### Как было до big bang (этапы T0–T5, M1) — ветка `stable`

| Экран | Состояние |
|---|---|
| 🖥️ **Стенд** | `/health` и `/version` (все поля сборки), параметры подключения, пути журналов, снимок реестров (файлы/нагрузки/модели/датасеты/задачи + записи: объединение, дубли, несопоставленные) на начало и окончание сессии, сравнение снимков |
| 🧪 **Сессия испытаний** | создание/старт/завершение/переоткрытие, поля сессии (наименование, программа-методика, объект, заказчик, организация, оператор, комиссия, цель, объём, условия, ограничения, критерии, подписи), история событий, артефакты испытаний, характеристики разметки, ручные вызовы консоли (`console_calls`), наблюдение за задачами (`tasks`, схема v4), файл сессии (выгрузка/загрузка), список сессий |
| 🧩 **Записи (RAW + markup)** | объединение реестра в записи по явному правилу (имя + время импорта) с показом правила и уверенности, KPI (записи, покрытие, дубли, без разметки), карточки с обеими частями и кандидатами, вкладки «Дубли» / «Несопоставленные» / «Прочие файлы» / «Манифест», выгрузка RAW/markup/пары-zip (крупные — с подтверждением), разбор разметки (записи/чанки), ручные решения оператора (`link`/`unlink`/`exclude`/`current`), манифест объединения CSV/JSON и сохранение в артефакты сессии |
| ⏱️ **Задачи** | монитор асинхронных задач: список задач сервера с фильтрами (`task_type`, `status`, период — переключателем, границы как дата-время: стенд отклоняет «чистую» дату), сортировкой «создана ↓» и клиентскими страницами; **статус строки** — пиктограмма с цветом (`▶`/`✔`/`✖`) и источником («карточка»/«снимок»/«опрос»), карточки видимых строк догружаются батчами с меткой `TC-TASK-02`; **архив задач** («завершена ≥ суток назад» — автоматически, кнопка «перенести завершённые» — по требованию оператора, задачи, вернувшиеся в работу, возвращаются из архива) и снимок статусов в `acceptance_data/task_snapshot.json`; компактные `id`/название/описание с полными значениями в панели строки; наблюдение (поллинг по интервалу, пауза/возобновление, снятие, `intermediate_result`), карточка `TaskWithRuntimes` с прогонами и историей переходов FSM-1, команды строго по `available_actions` (`pause`/`resume`/`interrupt`), диагностика `celery-test` с подтверждением, наблюдение за внешней задачей, привязка к проверке, замечание к API, артефакт, переходы в консоль и журнал |
| ✅ **Чек-лист проверок** | весь чек-лист пульта — **48 проверок** шести групп (`TC-SYS`, `TC-FILE`, `TC-REC`, `TC-LOAD`, `TC-TASK`, `TC-DS`): KPI каталога (`48 / 69`) и состава выборки по всем фильтрам, прогресс готовности, фильтры (группа, статус, класс, поиск по id/названию/требованиям, «только невыполненные»), таблица с вердиктами и диапазоном журнала, карточка проверки (шаги, ожидание, эндпоинты, негативные пробы, доказательства), запуск автоматических сценариев с пикерами цели (`task_id`/`file_id`/`load_id`/`dataset_id`, режим чтения состава, окна ожидания `fill`), групповой прогон безопасных (`tech`) проверок, ручные отметки оператора, выгрузка CSV и сохранение чек-листа в артефакты, замечание к API (для ожидаемых дефектов создаётся **автоматически** при статусе «блокировано API») |
| 🗂️ **Датасеты** | реестр с клиентскими фильтрами (`name`/`description`/`type`/`creation_date`) и выгрузкой перечня; создание только `__TEST__`-датасетов с учётом в сессии; **контур состава**: источник данных всегда подписан (серверный состав / факт наполнения пультом / гипотеза по реестру файлов / «недоступен»), сверка серверного состава с фактом, выгрузка состава в артефакты, замечание к API из шаблона P1; **наполнение**: пары RAW+markup из живых записей, подтверждение (NFR-T4), ответственный, `fill` → 202 без `task_id` → поиск задачи `dataset-fill` в `GET /api/tasks/` → наблюдение до терминального статуса; **уборка**: поштучное и массовое удаление тестовых датасетов, закрытие «хвостов» учёта сессии |
| 📡 **Консоль запросов** | все 40 операций 10 модулей из `SOM1.json` (реестр генерируется; контракт 17.09.2026), шаблоны пути/query/JSON/multipart, живой список задач для подстановки `task_id`, классы безопасности и подтверждения (префикс `__TEST__`, слово `DELETE` + ввод `id`, карточка запуска), результат со статусом/заголовками/телом/длительностью/размером и номером записи журнала, действия «в артефакты / к проверке / к монитору / замечание к API / повтор / очистить», вкладки «История вызовов» и «Операции API» |
| ✍️ **Замечания к API** | реестр замечаний с приоритетами P0/P1/P2: KPI, фильтры и поиск, карточка замечания, правка и удаление с подтверждением, 14 шаблонов известных дефектов (11 из комментариев заказчика + семантика фильтров периода, отсутствие статуса в списке задач, отсутствие сортировки), перспективные требования (бэклог), выгрузка Markdown/JSON/CSV и сохранение комплекта в артефакты сессии |
| 🔄 **Монитор обмена** | лента вызовов API: по контейнеру на обмен (метод, путь с query, статус, мс, метка проверки), **независимая подробность запроса и ответа** (`кратко`…`полностью`: сводка, заголовки, тело, JSON-дамп записи журнала), фильтры (только ошибки, метка, лимит) и порядок, копирование запроса в `curl`, **повтор с правками** (путь, query, JSON-тело), замечание к API из записи; рядом — **программа испытаний**: двухуровневый план (проверка + её вызовы), галочки (в том числе массово по выборке), режимы «проверки целиком / по вызовам», кнопки «▶ Выполнить следующую», «⏯ Авто с паузой», «⏸ Стоп», «↺ Сброс», вердикты «соответствует ожиданию», параметры вызова и предпросмотр `__TEST__`-данных |
| 🧾 **Журнал** | все обмены с сервером (метод, путь, статус, мс, размеры, метка проверки), фильтры (ошибки/метка/подстрока), заголовки и тела в деталях обмена, очистка, выгрузка JSON, скачивание JSONL сессии, хвост журнала текущего запуска и выбор журналов прошлых запусков (`logs/applog/`) |
| 📄 **Отчёт испытаний** | комплект отчётности одной кнопкой: KPI готовности и «чего не хватает» для подписания, предпросмотр markdown, отчёт `md` + машиночитаемый `json`, приложения `checks.csv` / `notes.csv` / `test_entities.csv` / `journal.jsonl` / манифест записей, выгрузка файлов и сохранение комплекта в артефакты сессии; 12 разделов (сессия, программа и объём, снимки стенда и их сравнение, сводка, детализация проверок с доказательствами, наблюдение за задачами и история FSM-1, датасеты и состав с признаками дублей, артефакты и `__TEST__`-сущности, замечания к API по приоритетам, перспективные требования, итог и подписи, приложения — раздел «Программа испытаний (план вызовов)» снят на этапе 2 big bang, его место займёт «Программа и объём» на этапе 5); отчёт строится **только по данным сессии и журнала** — сервер при формировании не опрашивается |

## Стек

| Слой | Технология |
|---|---|
| Веб-интерфейс | Streamlit ≥ 1.36 |
| HTTP-клиент | httpx ≥ 0.27 |
| Типы | Pydantic ≥ 2.6 (модели из `SOM1.json`) |
| Журналирование | loguru ≥ 0.7 (консоль + файл с ротацией + структурный JSONL) |
| Тесты/линт | pytest, ruff, mypy (640 тестов без сети + живые интеграционные по маркеру `integration`) |

## Структура

```
client/                 вендорная копия сетевого слоя + аддитивные tasks.py, datasets.py
lib/                    вендорная копия доменных хелперов (пары RAW+markup, FSM-1, полинг)
acceptance/             код пульта
  app.py                точка входа (навигация, уровень журнала, стартовые события)
  logging_setup.py      конфигурация loguru: консоль, logs/applog/<запуск>.log (ротация), session_<id>.jsonl
  glossary.py           глоссарий терминов: `$`-сущности стенда, подписи экранов
  http_log.py           LoggingTransport (httpx) + Journal (журнал обмена: заголовки, тела)
  session.py            модель сессии испытаний (**схема v7**: программа, очередь, история повторов)
  programme.py          программа сессии: объединение наборов по ИЛИ, покрытие, ревизии, утверждение
  sets.py               библиотека наборов проверок (check_sets.json): состав, порядок, ревизии
  queue.py              очередь прогона: снимок утверждённой ревизии, маркеры «следующая/выполняется/последняя»
  results.py            единый след результата и история повторов проверки
  runner.py             исполнение очереди: «Следующая», «Пачка tech», «Авто с паузой», карточка запуска
  labels.py             подписи и компактные представления (сборка, короткие id, длинные тексты)
  test_payloads.py      предпросмотр `__TEST__`-данных («что будет отправлено»)
  notes.py              реестр замечаний к API и перспективные требования
  records.py            объединение файлов в записи: пары, дубли, несопоставленные, манифест
  overrides.py          ручные решения оператора по записям (records_overrides.json)
  api.py                сборка клиента с логирующим транспортом, снимок стенда
  endpoints.py          реестр эндпоинтов API (генерируется из docs/SOM1.json)
  exchange.py           движок вызова консоли: результат вместо исключений
  tasks_monitor.py      монитор асинхронных задач: опрос, FSM-1, команды, история наблюдения
  task_snapshot.py      снимок серверных статусов задач и архив (task_snapshot.json)
  checks/registry.py    модели чек-листа (TC-…, классы tech/live/heavy/manual)
  checks/catalog.py     каталог проверок: 10 групп / 69 проверок, наполнено 5 групп (41, этапы T3/T4)
  checks/engine.py      движок проверки: метка TC-…, диапазон журнала, вердикт, сессия,
                        замечание по дефекту, артефакт, сводка и выборка чек-листа
  checks/runner.py      контекст и реестр сценариев, «сырые» пробы, evaluate/automate
  checks/system.py      сценарии TC-SYS-01…05 (доступность, версия, ошибки, неизвестный путь)
  checks/files.py       сценарии TC-FILE-01…14 (реестр, фильтры, скачивание, загрузка и круговой рейс)
  checks/records.py     сценарии TC-REC-01…06 (записи, дубли, манифест, разметка, версии)
  checks/loads.py       сценарии TC-LOAD-01…07 (реестр нагрузок, фильтры, отсутствие удаления)
  checks/tasks.py       автоматические сценарии проверок задач TC-TASK-01…08 (T4)
  report.py             комплект отчёта об испытаниях: md, json, приложения (без запросов к серверу)
  ui/nav.py             реестр 15 экранов (SCR-101…SCR-501) и пять групп навигации
  ui/state.py           runtime (журнал, клиент, клиент консоли), текущая сессия, кэш реестров и задач
  ui/screens/           экраны SCR-101…SCR-501: по модулю на экран (сейчас — скелеты макета)
  ui/components/        общие компоненты каркаса (трёхзонная раскладка и рендер скелета)
tests/                  unit (без сети), smoke (AppTest), integration (живой стенд)
tools/                  sync_vendor.ps1, sync_docs.py (копии документов постановки), probe_records.py
docs/                   ТЗ и чек-лист испытаний (копии постановки), отчёты этапов T1–T5 и «Монитор
                        обмена» (13), руководство пользователя (12), концепт и целевой комплект
                        (14–16: концепт, ТЗ, макет экранов), оценка сложности запросов (05)
mockup/                 офлайн-прототип целевого интерфейса (15 экранов SCR-101…SCR-501, включая
                        «Наборы проверок» и «Программу сессии»): index.html, styles.css,
                        app.js, demo-data.js (открывается в браузере)
acceptance_data/        сессии, журналы, отчёты, артефакты, решения оператора, архив задач (gitignored)
```

## Настройка и запуск

```powershell
.\run_pult.bat        # Windows: создаст .venv, поставит зависимости, откроет http://localhost:8502
./run_pult.sh         # Linux/macOS
```

Переменные окружения (`.env`, шаблон — `.env.example`):

| Переменная | Назначение | По умолчанию |
|---|---|---|
| `TRAINING_SERVER_BASE_URL` | адрес испытуемого сервера (обязательно) | — |
| `TRAINING_SERVER_TIMEOUT` | таймаут обычных запросов, с | 15 |
| `TRAINING_SERVER_DOWNLOAD_TIMEOUT` | таймаут скачивания файлов, с | 600 |
| `TRAINING_SERVER_DOWNLOAD_LIMIT_MB` | порог размера файла (подтверждение оператора), МБ | 200 |
| `PULT_LOG_LEVEL` | `DEBUG`/`INFO`/`WARNING`/`ERROR` | INFO |
| `PULT_LOG_BODIES` | сохранять тела запросов/ответов | true |
| `PULT_BODY_LIMIT` | лимит тела в журнале, символов | 4096 |
| `PULT_JOURNAL_MAX` | записей журнала в памяти | 5000 |
| `PULT_LOG_ENQUEUE` | асинхронная запись журнала (false — журнал не теряется при аварии) | false |
| `PULT_POLL_INTERVAL` | интервал поллинга задач, с (этап T4) | 2.0 |

## Журналирование (FR-T6)

* `acceptance_data/logs/applog/applog_<ГГГГММДД-ЧЧММСС>.log` — текстовый журнал
  приложения: **свой файл на каждый запуск** пульта (имя — момент старта процесса,
  ротация 10 МБ × 10, UTF-8), поэтому фрагмент ищется внутри одного запуска, а не в
  одной длинной ленте за все прогоны;
* `acceptance_data/logs/session_<id>.jsonl` — структурный журнал сессии: события
  `pult_started`, `session_*`, `stand_*`, `http_request`/`http_response`/`http_error`,
  `journal_cleared`, … с полями `session_id`, `check_id`, `module`, `event`, `payload`;
* каждый HTTP-обмен фиксируется логирующим транспортом: метод, путь, query, метка
  проверки, статус, длительность, размеры, `Content-Type`, тела (text/json/csv —
  до лимита; бинарные данные — только размером);
* события записей (этап T1): `records_loaded`, `records_refreshed`, `record_download`,
  `record_download_failed`, `record_markup_stats`, `record_override`,
  `record_override_reset`, `records_manifest_saved`, `artifact_saved`, `api_note_created`;
  HTTP-запросы экрана помечаются метками `TC-REC-01…TC-REC-05` (фильтр в журнале);
* события консоли и замечаний (этап T2): `console_request` (операция, класс безопасности,
  подтверждения, карточка запуска, статус, запись журнала), `console_response_saved`,
  `console_attachment`, `api_note_created`/`api_note_updated`/`api_note_removed`,
  `notes_export_saved`; вызовы консоли помечаются меткой проверки (`TC-…`, по умолчанию
  `TC-CONSOLE`) и дублируются в `console_calls` сессии; ручные записи истории сессии —
  `console_call`, `console_attachment`, `api_note*`;
* события монитора задач и чек-листа (этап T4): `task_registered`, `task_polled`/
  `task_status_changed`, `task_poll_error`, `task_command`, `task_test_started`,
  `tasks_archived`/`tasks_archived_auto`/`tasks_unarchived_auto` (архив задач в
  `acceptance_data/task_snapshot.json`),
  `check_started`/`check_finished`/`check_marked`; обмены монитора и проверок помечаются
  метками `TC-TASK-01…TC-TASK-08` (диапазон записей проверки виден в её карточке);
* журнал испытаний пишется синхронно (по умолчанию), чтобы не терялся при аварии;
* события чек-листа и отчёта (этап T3): `check_started`/`check_finished`/`check_marked`,
  `checklist_exported` (выгрузка чек-листа в артефакты), `api_note_auto_created`
  (замечание к API по ожидаемому дефекту), `test_entity_created`/`test_entity_deleted`
  (учёт `__TEST__`-сущностей для самоочистки), `markup_stats` (разбор разметки),
  `report_saved` (сохранение комплекта отчёта); обмены проверок помечаются метками
  `TC-SYS-01…TC-LOAD-07` — по ним в журнале можно отфильтровать запросы проверки.

## Тесты

```powershell
python -m pytest                       # unit + smoke (без сети)
$env:TRAINING_SERVER_BASE_URL = "https://energomera.ai-center.online"
python -m pytest -m integration        # живые проверки (только чтение)
python -m ruff check . ; python -m mypy client lib acceptance tests
```

Смоук-тесты запускают приложение через `streamlit.testing.v1.AppTest` с подменённым
транспортом httpx, поэтому не зависят от стенда. Интеграционные тесты T0–T5
**не выполняют** изменяющих и ресурсоёмких операций (кроме одного небольшого
markup-файла для проверки разбора разметки): консоль запросов проверяется только
на читающих операциях (`/health`, реестр файлов, 404 на скачивание неизвестного `id`),
а сценарий «создать `celery-test` и провести `pause → resume → interrupt`» включается
отдельной переменной:

```powershell
$env:PULT_LIVE_WRITE = "1"              # разрешение на изменяющие сценарии T4 и T3
```

Под этим флагом дополнительно выполняются:

* сценарий T4 «создать `celery-test` и провести `pause → resume → interrupt`»;
* проверки T3 с записью `TC-FILE-11/12/13` (`tests/integration/test_live_write.py`):
  негативные загрузки и круговой рейс `upload → download → сверка sha256 → delete`;
  после прогона тест убеждается, что в реестре не осталось файлов `__TEST__…` этой
  сессии, а в учёте сессии есть пары «создан/удалён» (NFR-T4, самоочистка).

Реестр эндпоинтов консоли генерируется из спецификации — при обновлении `docs/SOM1.json`
его нужно пересобрать:

```powershell
python -m tools.gen_endpoints           # пересобрать реестр 40 операций + ruff format
python -m tools.gen_endpoints --check   # сверка со спецификацией (для CI)
```

Контракт API зафиксирован: `docs/SOM1.json` — версия **17.09.2026** (34 пути / 40 операций /
49 схем / 10 тегов), предыдущая версия сохранена как `docs/SOM1.2026-08-31.json`. Актуальность
проверяет `tests/unit/test_contract_version.py`, разбор изменений и план приведения пульта —
`docs/09. Контракт API 17.09.2026 — изменения и план работ.md`. **Контракт не перезаписывается**
командой `python -m tools.sync_docs` (защита от отката версии): обновление — только с явным
флагом `--with-contract`.

Живой прогон контракта (этап **К2.1**) выполнен 18.09.2026 на сборке **`dev@83319ae`**:
30 из 41 проверки — 28 успех, 1 отказ (`TC-SYS-03`: неизвестный путь отвечает 405),
1 блокировано API (`TC-FILE-10`: не-ASCII имена), снимки стенда сняты. Отчёт, read-only
пробы, находки и остаток — `docs/10. Живой прогон контракта 17.09.2026 — К2-минимум.md`.

Живая проверка объединения без тестов:

```powershell
python -m tools.probe_records     # показатели записей, дубли, разбор разметки, дефект latin-1
```

## Этапы

| Этап | Содержание | Состояние |
|---|---|---|
| **T0** | каркас, журналирование, журнал обмена, сессия (черновик), стенд, аддитивные `client/tasks.py`, `client/datasets.py` | **выполнен** |
| **T1** | **«Записи (RAW + markup)»**: объединение реестра в записи, дубли, несопоставленные файлы, ручные привязки, актуальная версия, разбор разметки, манифест объединения, артефакты сессии | **выполнен** |
| **T2** | **консоль запросов** (все операции `SOM1.json` — на момент этапа 32, с контрактом 17.09.2026 — 40; генератор реестра, классы безопасности, карточка запуска, артефакты/заметки/повтор) и **реестр замечаний к API** (шаблоны + ручные, правка/удаление, выгрузки md/json/csv, перспективные требования); сессия schema v3 (`console_calls`) | **выполнен** |
| **T4** | **задачи и монитор** (FR-8/FR-T11): сессия schema v4 (`tasks`, история FSM-1), каталог проверок с группой `TC-TASK` (8 проверок), движок проверок, экран «⏱️ Задачи» (список, наблюдение, карточка, диагностика `celery-test`, внешняя задача), минимальный экран «✅ Чек-лист проверок», живой список задач в консоли | **выполнен** |
| T3 | чек-лист `TC-SYS`/`TC-FILE`/`TC-REC`/`TC-LOAD` (41 проверка, включая боевые с самоочисткой) и **отчёт об испытаниях** (md/json + приложения, без запросов к серверу), авто-замечания к API по ожидаемым дефектам | **выполнен** |
| T5 | датасеты (TC-DS-01…07): CRUD с самоочисткой, `fill` → задача `dataset-fill` (обходной путь: `task_id` не возвращается), контур учёта состава (серверный состав / факт пульта / гипотеза, sidecar `dataset_composition.json`), экран «🗂️ Датасеты», раздел отчёта «Датасеты и состав», сессия schema v5 (`dataset_composition`) | **выполнен** |
| **M1** | **«Монитор обмена» и программа испытаний** (пожелания 21.09.2026): лента вызовов с независимой подробностью запроса/ответа и заголовками, копирование `curl`, повтор с правками, двухуровневый план вызовов с галочками и вердиктами, «Выполнить следующую» / «Авто с паузой» / «Стоп» / «Сброс», терминология `$`-сущностей, предпросмотр `__TEST__`-данных, журналы по запускам, сессия schema v6 (`plan`), раздел отчёта 12 и `plan.csv` | **выполнен** |
| T6 | модели (TC-MOD): архитектуры, создание, загрузка `.h5`, список/карточка/удаление | — |
| T7 | обучение/проверка/экспорт ONNX (TC-TR, ресурсоёмкие — программа испытаний, FR-T10) | — |
| T8 | инференс (TC-INF): async по датасету и `inference/single` | — |
| T9 | отчёт: полный комплект (md/json + приложения), сравнение сессий, финализация с подписями | — |
| T10 | автотесты, документация, передача комплекта испытаний | — |
| **T11** | **AutoML** (`client/find_params.py`, помощники консоли, группа `TC-AUTO` — 8 проверок) | независимый блок в конце плана |

Порядок обязательных блоков — T5 → T6 → T7 → T8 → T9 → T10; AutoML вынесен в **T11**
(независимый блок): его доступность и ошибки не влияют на приёмку остальных модулей
(`docs/09` §10.2), а восемь операций контракта уже доступны из консоли после К0.

## Ограничения

* **Субдатасеты в пульте не используются** (решение заказчика 15.09.2026): экран
  работает только с записями (RAW + markup), дублями и несопоставленными файлами;
  субдатасет остаётся перспективным требованием к API (`notes.PROSPECTIVE_REQUIREMENTS`).
* Ресурсоёмкие проверки (`train`, async-инференс, `fill` больших датасетов) —
  полноправная часть **сессий испытаний**, но не автотестов пульта: автотесты
  выполняют только чтение и `POST /api/tasks/test` с малой длительностью.
* С этапа T2 пульт может выполнять **любую** операцию API (включая изменяющие и
  ресурсоёмкие), но только осознанно: класс безопасности виден на экране, изменяющие
  операции требуют подтверждения и префикса `__TEST__` у имён создаваемых сущностей,
  удаляющие — чекбокса, слова `DELETE` и повторного ввода `id`, ресурсоёмкие — карточки
  запуска. По умолчанию (в автотестах и живых прогонах этапа) выполняются только
  читающие операции и `POST /api/tasks/test`.
* Известные дефекты API (скачивание не-ASCII, отсутствие `Range`, `limit/offset`
  у файлов, `phase_connection`, связи «датасет ↔ файлы») фиксируются как
  «блокировано API» + замечание — это результат испытаний, а не ошибка пульта.
  По реестру на 15.09.2026 не скачиваются **108 из 129** записей (не-ASCII имена).
* Крупные RAW (до 2.1 ГБ) не скачиваются без явного подтверждения оператора;
  адрес испытуемого сервера и уровень журнала задаются в `.env`.
* Скрипт Streamlit выполняется при подключении браузера: файлы журнала за текущий
  запуск появляются после открытия UI (проверка — `python -m pytest` и открытие
  пульта в браузере).