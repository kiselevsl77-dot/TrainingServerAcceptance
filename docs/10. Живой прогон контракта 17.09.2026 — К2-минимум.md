# Живой прогон контракта 17.09.2026 — К2-минимум

> **О документе.** Приёмочный отчёт этапа К2 (обязательная часть): живой прогон пульта
> против стенда на контракте 17.09.2026 и фиксация того, что подтверждено, что осталось
> и какие правки внесены по находкам. AutoML-часть К2 (шаг 2.2) перенесена в этап T11
> и в этом документе не рассматривается.
>
> **Дата:** 18.09.2026. **Стенд:** `https://energomera.ai-center.online`.
> **Репозиторий:** `D:\VSC Python projects\TrainingServerAcceptance`.
> **Источники:** `apptestslog/26-09-17/` (сессия `20260917-133033-8e66` — прогон «до К0»),
> `apptestslog/26-09-18/` (сессия `20260918-140006-b6ed` — прогон после К0), read-only
> пробы и интеграционный тест `tests/integration/test_live_read.py`.

## 1. Итог этапа

| Что проверялось | Результат |
|---|---|
| Контракт 17.09.2026 соответствует живой сборке | **подтверждено**: `dev@83319ae` (build 17.09.2026T15:08:59Z) — та же сборка, что отдала спецификацию |
| Снятые требования Loads (`phase_connection`, `ph_n`) | **подтверждено**: в элементе реестра ровно `load_id`, `category`, `description`; неизвестный `ph_n` игнорируется (200, выдача та же) |
| `TaskType.FIND_PARAMS` | **подтверждено**: `GET /api/tasks/?task_type=find-params` → 200, `count = 0` (задач пока нет) |
| Чек-лист: группы T3/T4 на живом стенде | 30 из 41 проверки выполнено: **28 успех**, 1 отказ, 1 блокировано API, 2 пропущено |
| Снимки стенда и учёт ресурсов | сняты «начало» (14:00:18) и «окончание» (14:26:15); изменение реестров объяснено (+1 задача) |
| Автоматические замечания к API | 1 (P0, `TC-FILE-10`); прежние P0 по Loads закрыты контрактом и сняты |
| Регрессия пульта | `tests/integration/test_live_read.py` — **8 passed**; unit/smoke — без изменений поведения |

**Вывод:** К0 подтверждён живым прогоном на новой сборке; блокирующих дефектов пульта нет.
Не выполнены 11 проверок, требующих боевых действий и ручных отметок (§7) — это
плановый остаток К2, а не дефект.

## 2. Стенд и сборки

| Дата | Сборка | Откуда |
|---|---|---|
| 15–17.09.2026 | `dev@58ac72f` (build_date 2026-09-14T12:36:24Z) | сессия `20260917-133033-8e66`, отчёты T1–T3 |
| **18.09.2026** | **`dev@83319ae`** (build_date **2026-09-17T15:08:59Z**) | сессия `20260918-140006-b6ed`, пробы §5 |

Смена сборки — существенный факт для отчётности: контракт `docs/SOM1.json`
(получен 17.09.2026) описывает **сборку `dev@83319ae`**, а прогон 17.09.2026 шёл на
предыдущей (`dev@58ac72f`). Поэтому в отчёте об испытаниях сборку сессии нужно сверять
с `GET /version`, а результаты сессии 17.09.2026 считать историческими.

## 3. Прогон 17.09.2026 — база сравнения (до К0)

* 30 из 41: успех 26, отказ 1 (`TC-SYS-03`), «блокировано API» 3
  (`TC-FILE-10`, `TC-LOAD-02`, `TC-LOAD-03`), пропущено 2 (`TC-TASK-03/06`).
* Замечания: 2 P0 (не-ASCII имена файлов; «нет `phase_connection`, `ph_n` не фильтрует»).
* Снимков стенда нет → отчёт неполон («сравнение реестров не построить»).
* Данные: 265 файлов, 129 записей, 62 нагрузки, 5 моделей, 11 задач.

Этот прогон ценен как «до»: он подтвердил дефект `TC-SYS-03` (405 вместо 404) и
зафиксировал факт «`ph_n` игнорируется», который после К0 стал штатным поведением
(параметр убран из контракта).

## 4. Прогон 18.09.2026 — после К0

| Показатель | 17.09 | **18.09** |
|---|---|---|
| Сборка сервера | `dev@58ac72f` | **`dev@83319ae`** |
| Выполнено | 30/41 | 30/41 |
| Успех / отказ / блокировано / пропущено | 26 / 1 / 3 / 2 | **28 / 1 / 1 / 2** |
| Снимки стенда | нет | **«начало» и «окончание»** |
| Замечания к API | 2 (2 P0) | **1 (P0)** |
| Артефакты сессии | 2 | **6 (комплект отчёта)** |
| Задачи на наблюдении | 0 | 3 |

По группам: `TC-SYS` 5/6 (1 отказ), `TC-FILE` 11/14 (1 блокировано), `TC-REC` 5/6,
`TC-LOAD` **7/7**, `TC-TASK` 2/8 — «выполнено 2» объясняется тем, что 4 проверки TASK
требуют выбранной задачи (см. находку №5), а 2 — ручные.

**Снимок стенда (начало → окончание):** datasets 6 → 6, files 266 → 266,
loads 62 → 62, models 6 → 6, tasks 13 → **14** (+1 — диагностическая задача, созданная
из консоли). Записи: 129 (дублей 128, баз 61, покрытие разметкой 100 %),
RAW 41 544 589 031 Б, markup 167 311 346 Б — совпадает с отчётами T1–T3.

**Консольные вызовы сессии (5, все через пульт):**

| Время | Вызов | Результат |
|---|---|---|
| 14:13:03 | `POST /api/data/file` (`__TEST__.txt`, 6 Б) | **422** `file_type: Field required` — поле формы не было заполнено (находка №4) |
| 14:16:09 | `POST /api/data/file` (`__TEST__background.raw.csv`, 197 309 Б, `file_type=RAW`) | 201 — файл создан |
| 14:16:47 | `GET /api/data/files` | 200 — файл найден в реестре |
| 14:18:15 | `DELETE /api/6098ffed-2e0d-4aad-93d5-c7642562af72` | 200 — убран за собой |

То есть круговой рейс «загрузить → найти → удалить» выполнен вручную; проверка
`TC-FILE-13` фиксирует то же автоматически и пишет пару «создан/удалён» в учёт
`__TEST__`-сущностей (в отчёте 18.09 `test_entities` пуст, потому что боевые проверки
файлов не запускались — §7).

**Замечание сессии (P0):** скачивание файлов с не-ASCII именами (`GET /api/data/file/{id}/download`
→ 404 `'latin-1' codec can't encode characters…`) — дефект стенда, подтверждён повторно.

## 5. Read-only пробы контракта (18.09.2026)

Пробы выполнены «сырым» клиентом (только `GET`, состояние стенда не менялось);
интеграционный тест — `python -m pytest tests/integration/test_live_read.py -m integration`
(**8 passed**).

| Проба | Ответ | Что подтверждает |
|---|---|---|
| `GET /health` | 200 `{"status":"OK","environment":"dev"}` | доступность и признак окружения |
| `GET /version` | `dev@83319ae`, build_date 2026-09-17T15:08:59Z | сборку объекта испытаний |
| `GET /api/loads/list?limit=1` | 200, `result_size = 62`, поля `{load_id, category, description}` | снятое требование фазы (К0, `TC-LOAD-02`) |
| `GET /api/loads/list?limit=1&ph_n=Ph_A` | 200, **та же** выдача | неизвестный параметр игнорируется (`TC-LOAD-03`) |
| `GET /api/loads/list?limit=1&offset=0|1` | разные записи (`__OTHER__` → `3Дпринтер`), `offset` в ответе | срез `limit`/`offset` работает (`TC-LOAD-01`) |
| `GET /api/tasks/?task_type=find-params` | 200 `{"tasks":[],"count":0}` | новый тип задачи принят сервером |
| `GET /api/datasets/` | 200, 6 датасетов (`skfu-train`, `leti-train`, `skfu-test`, `leti-test`, `synt-train`, `synt-test`), все `direct_fill` | данные для T5 и для T11 (итерация отбора) |
| `GET /api/ml_models/models` | 200, 6 моделей (`stft_mlp`, `phase_type=single`), **`signals = null`** у всех | данные для T6/T7; замечание P2 живо |
| `GET /api/ml_models/architectures` | 200 (`cnn_1d` с `default_config` и `suitable_for_phases`) | данные для `TC-MOD-01` |
| `GET /api/ml_models/find_params/population` | **200 `[]`** | блок AutoML **есть в сборке** (для T11: «недоступно в сборке» — не наш случай) |
| `GET .../population/{uuid}` и её подресурсы `/grid.csv`, `/mutated_models`, `/selections` | 404 `{"detail":"Population … not found"}` | единый формат ошибок AutoML; `grid.csv` для несуществующей популяции отдаёт JSON, а не CSV |
| `GET /api/__test_unknown__` | **405**, заголовок `allow: DELETE` | причину отказа `TC-SYS-03` (находка №1) |

## 6. Находки этапа

| № | Находка | Тип | Статус |
|---|---|---|---|
| 1 | `TC-SYS-03`: неизвестный путь `/api/__test_unknown__` отвечает **405** (`allow: DELETE`) вместо 404 — корневой маршрут `DELETE /api/{file_id}` перехватывает `/api/{file_id}` | **дефект API** (подтверждён на обеих сборках) | открыт; замечание в реестре, ожидаемый отказ проверки |
| 2 | `TC-FILE-10`: скачивание файлов с не-ASCII именами → 404 `latin-1` | **дефект API (P0)** | открыт; замечание формируется автоматически |
| 3 | `phase_connection` / `ph_n` в Loads | закрыто контрактом 17.09.2026 | закрыто; в сессии 17.09 остались **исторические** статусы «блокировано API» и замечание P0 — при чтении старых отчётов учитывать |
| 4 | `POST /api/data/file` из консоли: форма не требовала `file_type` → живой 422 при пустом поле | **дефект пульта (UX)** | **исправлен** (§8) |
| 5 | Пикер задачи в карточке проверки показывал только наблюдаемые задачи сессии → `TC-TASK-03/06` «пропущены» при 14 задачах на стенде | **дефект пульта (UX)** | **исправлен** (§8) |
| 6 | AutoML: операция создания популяции «одним вызовом» (`POST /api/ml_models/find_params/population`) отсутствует в контракте, хотя `GET` на этом пути обслуживается | расхождение спецификации (P1) | учитывается в T11 (проба + замечание) |
| 7 | `signals` пустой у всех моделей; `/api/data/files` игнорирует `limit`/`offset` | замечания к API (P2) | открыты, как и в отчётах T1–T3 |

## 7. Что осталось в К2 (11 проверок)

Не выполнены 9: `TC-SYS-06` (manual), `TC-FILE-11/12/13` (live — запись файлов),
`TC-REC-03` (manual), `TC-TASK-01/04/05` (live — задачи и FSM-1), `TC-TASK-08` (manual).
Пропущены 2: `TC-TASK-03/06` (нужен выбранный `task_id`).

Порядок и подробные инструкции (по экранам, полям и кнопкам):

1. **Файлы (`TC-FILE-11/12/13`)** — «Чек-лист проверок» → группа `TC-FILE` → карточка проверки
   → заполнить карточку запуска (цель, данные, ответственный, подтверждение) → «▶ Запустить проверку».
   Проверки сами формируют `__TEST__<uuid>.csv`, проверяют отклонение по колонкам/MIME и
   выполняют круговой рейс с самоочисткой.
2. **Задачи (`TC-TASK-01`, затем `03`, `04`, `05`, `06`)** — «Задачи» → вкладка «🩺 Диагностика»
   → длительность 60 с → подтверждение → «▶ Запустить celery-test»; далее «Чек-лист проверок» →
   выбрать проверку → в селекторе «Задача для сценария проверки» выбрать созданную задачу
   (пикер теперь показывает и список сервера) → «▶ Запустить проверку». Для `TC-TASK-04` перед
   запуском выполнить `pause` и `resume` в карточке задачи, дождавшись статусов
   «Выполняется» → «Приостановлена» → «Выполняется»; для `TC-TASK-05` — `interrupt`.
3. **Внешняя задача (`TC-TASK-08`)** — запустить задачу вне пульта
   (`curl.exe -s -X POST "https://energomera.ai-center.online/api/tasks/test?duration=60"`
   или из основного UI) → «Задачи» →
   вкладка «🔗 Внешняя задача» → вставить `task_id` → «⏱️ Поставить на наблюдение» →
   «✅ Подтвердить наблюдение внешней задачи (TC-TASK-08)».
4. **Ручные отметки (`TC-SYS-06`, `TC-REC-03`)** — сверить сборки `dev@58ac72f` (14.09) и
   `dev@83319ae` (17.09) и подтвердить различимость по `revision`/`build_date`; выполнить
   ручную перепривязку пары на экране «Записи» и зафиксировать её в `records_overrides.json`.
5. **Финал** — «Стенд» → «📸 Снять снимок» (окончание) → сформировать и сохранить комплект
   отчёта. Ожидаемый итог: **41 из 41 выполнено**, отказ — только `TC-SYS-03`,
   «блокировано API» — только `TC-FILE-10`, `test_entities` — пары «создан/удалён», `pending = 0`.

Проверки, требующие боевых операций на общем стенде, допустимы и по регламенту T3/T4:
создаются только `__TEST__`-сущности, все с самоочисткой.

## 8. Правки пульта по итогам этапа

| Файл | Что изменено |
|---|---|
| `acceptance/ui/pages/console.py` | multipart-форма: поле «Тип файла (file_type) \*» помечено обязательным, запрос не отправляется при пустом значении (подсказка «сервер требует `file_type`…»), кнопка «▶ Выполнить запрос» блокируется, пока тело неполно (`ready = False` для multipart). Причина — живой 422 из сессии 18.09.2026 (находка №4) |
| `acceptance/ui/pages/checks.py` | пикер задачи: константа `TASK_PICKER_LIMIT = 50`, помощники `_server_task_options()` (живой список задач сервера) и `_ensure_task_observed()` (постановка выбранной задачи на наблюдение перед сценарием, `register_task`/`register_external` + один опрос); `_render_task_picker` сначала берёт наблюдаемые задачи, а при их отсутствии — список сервера с пояснением (находка №5) |
| `tests/unit/test_console_page.py` | +3 теста: multipart без файла, multipart без `file_type`, multipart с файлом и типом (`form_data()` = `file_type` + `description`) |
| `tests/unit/test_checks_page.py` | новый файл: 12 тестов — список задач сервера (в том числе недоступный), фолбэк пикера, приоритет наблюдений, подсказка при отсутствии задач, постановка на наблюдение (серверная задача / неизвестная как внешняя / `TC-TASK-08` как внешняя / без задачи) |

Проверки после правок: `pytest tests/unit/test_checks_page.py tests/unit/test_console_page.py -q`
→ **30 passed**; `ruff check`, `ruff format --check`, `mypy` — без замечаний;
`pytest tests/integration/test_live_read.py -m integration` против стенда → **8 passed**.

## 9. Факты стенда для следующих этапов

* **Датасеты (T5):** 6 сущностей `direct_fill` — `skfu-train` (`06b5d3f3-eedb-450f-afc8-2ef33f6b7cf9`),
  `leti-train` (`af4951b0-a98a-4d4e-a391-c973edab48a0`), `skfu-test`
  (`3f403a48-1d11-4df8-8885-4773ad41bac4`), `leti-test` (`203f929c-e515-4a33-9267-8bc63838e083`),
  `synt-train` (`1ad1f913-0425-4c57-9a3d-11e8c8420ff2`), `synt-test`
  (`c90e8030-576c-4b94-98a4-1613e0068f81`).
* **Модели (T6/T7):** 6 моделей `stft_mlp` / `phase_type = single`, `signals` пуст —
  `TC-MOD-06` фиксирует это как замечание P2; материал для `TC-TR-*` есть.
* **Файлы и записи (T1–T3):** 266 файлов (RAW 129, LOADS 129, H5 6, REPORT_ZIP 2), 129 записей,
  128 в дублях, 61 база, покрытие разметкой 100 %; 3 файла с не-ASCII именами недоступны (P0).
* **Задачи (T4):** 14 задач, типы `dataset-fill`, `training`, `celery-test`; `find-params` пока 0.
* **AutoML (T11):** блок доступен, популяций нет (`[]`) — значит happy-path создаётся с нуля,
  а «каскадный `DELETE`» проверяется на первой же `__TEST__`-популяции.

## 10. Что дальше

1. Выполнить шаги §7 в UI (11 проверок) — получить сессию «41 из 41» и приложить её к комплекту.
2. Перейти к **T5 (датасеты)**: экран датасетов, сценарии `TC-DS-01…07`, `fill` → задача
   `dataset-fill` через монитор, документ этапа `docs/11`.
3. Далее по плану: T6 → T7 → T8 → T9 → T10, затем **T11 (AutoML: К1 + К2.2)** — как
   независимый блок в конце.

**Изменения этой сессии (для истории):** `docs/10` (новый), `acceptance/ui/pages/console.py`,
`acceptance/ui/pages/checks.py`, `tests/unit/test_console_page.py`,
`tests/unit/test_checks_page.py` (новый), `docs/README.md`, `README.md`, `docs/09` (статус К2.1).

