Собрал полный инвентарь. Ниже — что именно нужно сделать в существующем коде и документации для соответствия новому контракту, и как зафиксировать контракт как актуальный. Текущие факты: дерево чистое (`2803235`), `python -m tools.gen_endpoints --check` → «реестр актуален: 32 операций» (то есть пульт сейчас соответствует **старой** спеке), 477 тестов зелёные.

## Шаг 1. Фиксация контракта как актуального

Ключевое решение: **каноническое имя оставляем `docs/SOM1.json`** — на него ссылаются 30+ мест в коде и документации (`tools/gen_endpoints.py:27`, тесты, докстринги `client/*.py`, `acceptance/endpoints.py:3`, экран консоли `console.py:1181`, `README.md:50`, `docs/01`, `docs/05`). Переименование в `api_17_09_26.json` потребовало бы правки всех этих ссылок без выигрыша.

| #    | Действие                                                     | Файл/результат                              |
| ---- | ------------------------------------------------------------ | ------------------------------------------- |
| 1.1  | Историчная копия текущего контракта из `docs/SOM1.json` (69 353 Б, sha `05dd6e2f…d06`) | `docs/SOM1.2026-08-31.json`                 |
| 1.2  | Актуальный контракт: содержимое `docs/api_17_09_26.json` (107 765 Б, sha `50f8ed29…b11`) → `docs/SOM1.json` | `sha256(docs/SOM1.json)` = `50f8ed29…c6b11` |
| 1.3  | Полученная поставка остаётся как есть (доказательство «как получено») | `docs/api_17_09_26.json` — без изменений    |
| 1.4  | В `docs/README.md` вместо текущего абзаца-примечания — таблица «Версии контракта» (дата, файл, размер, SHA-256, состав 27/32/34/6 → 34/40/49/10, где применён) | `docs/README.md`                            |
| 1.5  | **Стоп-тест актуальности контракта**: SHA-256 `docs/SOM1.json`, состав (34 пути / 40 операций / 49 схем / 10 тегов), наличие 8 AutoML-путей и `text/csv` у `grid.csv` | новый `tests/unit/test_contract_version.py` |
| 1.6  | Защита от отката: `tools/sync_docs.py:29` сейчас копирует `SOM1.json` **из постановки**, где пока версия 31.08.2026 → слепая синхронизация вернёт старую спеку. Правка: искать свежий `api_*.json` и копировать его как `SOM1.json`, с печатью SHA-256 и предупреждением при расхождении с записанным значением | `tools/sync_docs.py`                        |

Без п. 1.5–1.6 «актуальность» контракта держится на честном слове: один запуск `python -m tools.sync_docs` откатит её назад.

## Шаг 2. Код: обязательные правки для соответствия

**Генератор реестра (`tools/gen_endpoints.py`)**
- `_response_kind` (стр. 274–286): добавить `text/csv` → новый вид (`csv`), иначе `GET …/grid.csv` останется `json` (проверено: `--report` на новом контракте так и показывает);
- `HEAVY_PATHS` (стр. 36–41): += `POST /api/ml_models/models/{model_id}/find_params/population`, `POST /api/ml_models/find_params/population/{population_id}/selection`;
- `ENDPOINT_NOTES` (стр. 53–73): заметки для 8 новых операций (нет one-call POST из описания; `selection` → 202 + задача `find-params`; каскадное удаление; CSV-выгрузка; свободные `object`; мутанты не наследуют веса; список популяций без пагинации);
- шапка §docstring (стр. 1–7, «(32 операции)») и `render_registry` (стр. 366) — формулировки.

**Реестр (`acceptance/endpoints.py`)**
- перегенерировать: `python -m tools.gen_endpoints` → **40 операций / 10 модулей**;
- объявить `RESPONSE_CSV` рядом с `RESPONSE_JSON/BINARY/EMPTY` (стр. 66–68) и решить, считается ли CSV «файлом» (`is_binary`, стр. 194–196);
- docstring стр. 3–7: «все 32 операции» → 40.

**Клиент (`client/schemas.py`, `client/loads.py`)**
- `TaskType` (стр. 43–50): добавить `FIND_PARAMS = "find-params"` (иначе фильтр «Тип задачи» на экране «Задачи» его не покажет);
- `LoadDevice` (стр. 125–131): **удалить `phase_connection`** — сейчас поле обязательное, и `POST /api/loads` уйдёт с лишним ключом (риск 422);
- `UpdateLoadRequest` (стр. 134–139): удалить `phase_connection`;
- `LoadItem` (стр. 142–156) и модульный docstring `client/loads.py` (стр. 1–21) + `Note` у `list_loads` (стр. 56–58): переписать замечания — `ph_n` из контракта убран, `phase_connection` не требуется, схема ответа по-прежнему пустая;
- `create_load` (стр. 75–90) / `update_load` (стр. 92–109): убрать параметр `phase_connection` (в UI не вызываются — задел UC-07/UC-08).

**Замечания (`acceptance/notes.py`)**
- `KNOWN_DEFECTS` (стр. 176–185): P0 по Loads переформулировать («исправлено контрактом 17.09.2026») либо снять из активных;
- `DEFECT_BY_CHECK` (стр. 377–386): убрать `TC-LOAD-02`/`TC-LOAD-03` — иначе движок продолжит автоматически создавать замечание «блокировано API», которого больше нет;
- `MODULES` (стр. 29–38): добавить группу для AutoML-замечаний (решение: `ML models: AutoML` отдельным пунктом).

**Чек-лист (`acceptance/checks/catalog.py`, `loads.py`)**
- `TC-LOAD-02` (стр. 584–602) и `TC-LOAD-03` (стр. 603–618): переформулировать под новый контракт (сверка состава полей с контрактом; проба неизвестного параметра `ph_n` с фиксацией факта), убрать `blocked_by_api` и «ожидаемо»;
- `loads.py`: `_fields` (стр. 122–143), `_ph_n_filter` (стр. 146–182), `AUTOMATIONS` (стр. 397–406), вводный docstring (стр. 1–14);
- `PLANNED_GROUPS` (стр. 42–53): решить судьбу модуля `ML models` у групп `TC-MOD`/`TC-TR`/`TC-INF` (в реестре такого модуля больше нет, но у пульта это домен проверок, а не тег).

**UI — правок почти не требует** (проверено): селектор модулей `console.py:130` берёт `ep.module_names()` и сам покажет 10 модулей; фильтры модулей на экранах замечаний/проверок/задач (`notes.py:182,283`, `checks.py:664`, `tasks.py:1205`) — из `notes.MODULES`. Нужно только: подписи «все операции контракта» (`console.py:82,1179–1182`), отображение CSV-ответа во вкладке «Операции API» (`console.py:1187–1213`) и проверка ширины длинных подписей.

**Документация (числа и формулировки)**

| Файл                        | Что менять                                                   |
| --------------------------- | ------------------------------------------------------------ |
| `README.md`                 | стр. 21 (32 операции / 6 модулей → 40 / 10), 163 («реестр 32 операций»), 179, 34 (число тестов); стр. 50/32 про `docs/SOM1.json` — остаётся верным |
| `docs/01. ТЗ …`             | стр. 29, 448, 500 (сгруппировать `ML models: initialization/training/testing/inference/AutoML`) |
| `docs/02. Чек-лист …`       | стр. 91–97 (`TC-LOAD`), 171–180 (сводка групп), 188–193 (ожидаемо блокированные: минус два P0) |
| `docs/05. Сложность …`      | заголовок 1, шкала 48–53, заголовок §4 (стр. 70), **+8 строк таблицы** с модулями буквально (`ML models: AutoML`), §5 — разбор AutoML-тел |
| `docs/04. Этап T2 …`        | стр. 4, 19 — исторические значения, снабдить пометкой «на момент этапа» |
| `docs/SOM1. Комментарии.md` | §1 (состав 34/40/49/10), переоценка P0 по Loads, новые находки §7.1–7.6 док. 09, ссылка на `docs/09` |
| `docs/09. …` (создан)       | при исполнении — заменить «предлагаемое» на факт и дописать номера коммитов |
| `llm_tasks/*.md`            | исторические рабочие записи — не трогаем                     |

## Шаг 3. Тесты-сторожа (точные утверждения, которые упадут)

| Файл                                        | Что именно упадёт                                            |
| ------------------------------------------- | ------------------------------------------------------------ |
| `tests/unit/test_endpoints.py`              | стр. 62 `len(ep.ENDPOINTS) == 32`; стр. 75 `len(ep.MODULES) == 6`; стр. 96–106 наборы `heavy`/`destructive`; стр. 184 note Loads содержит `phase_connection` |
| `tests/unit/test_request_complexity_doc.py` | стр. 32 `LEVELS_EXPECTED`; стр. 33–43 `LEVELS_HARDEST`/`LEVELS_HARD`; стр. 49 `OPERATIONS_IN_TABLE = 32`; стр. 113 сверка числа строк; стр. 125 сверка модулей с `spec.module` |
| `tests/unit/test_api_notes.py`              | стр. 73 `len(templates) == 14`; стр. 77–83 набор модулей шаблонов |
| `tests/unit/test_checks_catalog.py`         | стр. 48 `IMPLEMENTED_GROUPS`; стр. 53–59 `CLASSES_BY_GROUP`; стр. 212 `len(names) == 39`; стр. 235–240 список `blocked_by_api` (уходит `TC-LOAD-02/03`) |
| `tests/unit/test_checks_loads.py`           | стр. 208–234 (`fields` blocked без `phase_connection`), стр. 240–265 (`FakeLoads(ignore_ph_n=…)`, `query == "ph_n=Ph_A"`, `load_record(phase_connection=…)`) |
| `tests/unit/test_notes.py`                  | стр. 74, 83 (`phase_connection` в заголовке дефекта)         |
| `tests/unit/test_report.py`                 | стр. 144–145, 256, 355 (вердикты и заголовки с `phase_connection`) |
| `tests/unit/test_session.py`                | стр. 137, 141 (замечание «Дефект: нет phase_connection»)     |
| новые                                       | `tests/unit/test_contract_version.py` (шаг 1.5); при К1 — `tests/unit/test_checks_automl.py` |

## Шаг 4. Порядок исполнения и проверки

1. Контракт (1.1–1.4) → 2. `sync_docs` + стоп-тест (1.5–1.6) → 3. генератор + `RESPONSE_CSV` + `HEAVY_PATHS`/заметки → 4. `python -m tools.gen_endpoints` (40 операций) → 5. клиент (`TaskType`, `LoadDevice`, `UpdateLoadRequest`) → 6. `notes.py` + `catalog.py` + `checks/loads.py` → 7. тесты → 8. документы → 9. контроль: `python -m pytest tests -q` (ожидаемо 477+ passed), `python -m tools.gen_endpoints --check` (→ «40 операций»), `ruff check`. Живой прогон (`/version`, `?ph_n=`, `POST /api/loads` без `phase_connection`, `task_type=find-params`) — шаг К2, как в `docs/09` §8.

## Решения, которые нужны от вас перед исполнением (даю рекомендуемые по умолчанию)

1. схема фиксации: **`docs/SOM1.json` = актуальный, `docs/SOM1.2026-08-31.json` = архив** (рекомендую) — или другое имя/каталог `docs/archive/`;
2. `docs/api_17_09_26.json` оставляем в `docs/` (рекомендую) — или переносим в архив;
3. CSV: **новый вид `RESPONSE_CSV`** с показом как файла (рекомендую) — или точечный `RESPONSE_OVERRIDES`;
4. замечание P0 по Loads: **оставить в реестре со статусом «исправлено контрактом 17.09.2026»** (рекомендую, история в отчётах T1–T3) — или удалить из активных;
5. `notes.MODULES`: **добавить `ML models: AutoML`** и оставить существующий пункт `ML models` (рекомендую) — или приводить все 5 подтегов;
6. добавить стоп-тест `tests/unit/test_contract_version.py` — **да** (рекомендую).

Правки кода и документов в plan mode не выполняются, поэтому прошу **переключите на Act mode (toggle to Act mode)** — и я сразу выполню шаги 1–3 (фиксация контракта + генератор/реестр 40 операций + клиент + чек-лист/замечания + тесты + документы), а затем отчитаюсь прогоном `pytest -q` и `--check`. Если по любому из 6 решений нужен другой вариант — напишите, учту до начала.