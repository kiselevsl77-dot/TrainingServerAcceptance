Готово. Все проверки зелёные.

## Что сделано

**1. Новый документ — `docs/05. Сложность формирования запросов в консоли.md`** (286 строк, стиль отчётов T1/T2)

| Раздел | Содержание                                                   |
| ------ | ------------------------------------------------------------ |
| §1–§2  | что оценивается (метод запроса ни о чём не говорит — важнее источник значений) и 4 фактора сложности: источник значений, длина тела, структура тела, ограничения инструментов |
| §3     | шкала **S1–S4** (🟢 тривиально / 🟡 нужны данные из живого реестра / 🟠 много полей, вложенность, нет справочников / 🔴 вручную невоспроизводимо) с критериями и итогом: **10 + 15 + 4 + 3 = 32** |
| §4     | **главная таблица всех 32 операций**: №, модуль, метод и путь, класс безопасности, тело, сложность, «что нужно подготовить и откуда», обоснование |
| §5     | разбор 6 сложных случаев с примерами тел: `datasets/fill`, `inference/single`, `models/upload`, `POST /api/ml_models/models`, `train`, `POST`/`PUT /api/loads` |
| §6–§7  | что уже помогает в консоли T2 и какие помощники нужны, привязанные к этапам T5–T8 |
| §8     | трассировка на `TC-*` (TC-FILE-13, TC-DS-05/06, TC-MOD-02/03/04/07, TC-TR-01/02, TC-INF-01…04, TC-TASK-04/05/06/08, TC-CLEAN-01) |
| §9     | ограничения, выявленные при оценке (в т. ч. **находка по самому пульту**: `MultipartPayload` не умеет отправлять поле `metadata`, поэтому `POST /api/ml_models/models/upload` из консоли сегодня не формируется вовсе) |
| §10    | правило синхронизации документа с реестром/SOM1.json         |

**Ключевые оценки** (по фактическим данным, срез 15.09.2026: 265 файлов, 129 записей, 128 дублей, 62 нагрузки, 6 датасетов, 5 моделей):

- 🔴 **S4 (3):** `POST /api/datasets/fill/{dataset_id}` — **129+129 UUID** в одном теле (~11 КБ; реальный прогон сервера — 52+52 ≈ 4,5 КБ), пары RAW+markup собирает оператор из дублей; `POST …/inference/single` — 6 base64-сигналов по строке RAW-CSV плюс ADC-параметры (`U_mult` 721, `I_mult` 25000, `signal_bytes` 3, `byte_order` Little); `POST /api/ml_models/models/upload` — обязательное поле `metadata` (JSON-строкой), которого консоль не отправляет.
- 🟠 **S3 (4):** `POST`/`PUT /api/loads` (перечень фаз и категорий в API не описан, `phase_connection` вообще не отдаётся списком — P0), `POST /api/ml_models/models` (вложенные `signals`/`signal_aliases`, свободный `config`, base64 `pruning_mask`), `train` (9 полей с диапазонами, `additionalProperties: false`, вложенный `quantization`).
- 🟡 **S2 (15)** — всё, где нужен `id`/`task_id` из живого реестра, файл на диске, `threshold` или подтверждения; 🟢 **S1 (10)** — `/health`, `/version`, чтения реестров, `POST /api/datasets/`, `POST /api/tasks/test`.

**2. Новый тест — `tests/unit/test_request_complexity_doc.py`** (11 проверок): таблицу §4 нельзя развести с реестром — набор операций и нумерация, модуль, вид тела, класс безопасности, обязательное назначение уровня из шкалы, зафиксированное распределение уровней и состав S3/S4, «тяжёлые» операции не S1, заполненность колонок «что подготовить»/«обоснование», наличие разбора для S4, существование всех упомянутых `TC-*` в чек-листе, регистрация документа в `docs/README.md`.

**3. Правки перечней:** `docs/README.md` — строка на новый документ; корневой `README.md` — состав каталога `docs/` и актуальное число тестов (193).

## Проверки

| Проверка                                           | Результат                    |
| -------------------------------------------------- | ---------------------------- |
| `python -m pytest -q`                              | **193 passed** (11 новых)    |
| `python -m tools.gen_endpoints --check`            | реестр актуален: 32 операции |
| `python -m ruff check acceptance tools tests`      | All checks passed            |
| `python -m ruff format --check`                    | 93 файла отформатированы     |
| `python -m mypy acceptance client lib tests tools` | Success, 78 файлов           |

Замечания для работы:
- в `.venv` не было dev-зависимостей, я поставил их: `python -m pip install -r requirements-dev.txt` (pytest, ruff, mypy) — иначе тесты/линт не запускаются;
- живой стенд не затрагивался: ни одного изменяющего запроса не отправлялось, документ построен на спецификации, реестре операций и ранее зафиксированных фактах;
- найденное ограничение консоли (`metadata` в multipart) стоит учесть при этапе **T6 (модели)** — это единственное место, где операция API сегодня недоступна из пульта даже с подтверждениями.