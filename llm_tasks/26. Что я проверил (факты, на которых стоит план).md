## Что я проверил (факты, на которых стоит план)

| Проверка                                 | Результат                                                    |
| ---------------------------------------- | ------------------------------------------------------------ |
| Ветка/состояние                          | `master` == `origin/master`, этап 1b закрыт (799 passed / 3 skipped) |
| Реестр целевых экранов                   | `mockup/app.js` стр. 210–238 + `docs/16` §0 и §12: 15 экранов, 5 групп, фазы — источник для `ui/nav.py` |
| Гейт этапа 2 в плане                     | `llm_tasks/23…md` §5 стр. 207: «`app.py`, `ui/nav.py` (5 групп, `SCR-01…15`), `ui/state.py`, 15 скелетов; удаление `plan.py`, `plan_runner.py`, `ui/pages/**`, `plan_panel.py`, поля `plan`» → гейт: pytest, smoke «15 экранов», `gen_endpoints --check` |
| **Зависимость, которой не было в плане** | `acceptance/report.py` стр. 172/179/183/913/978 реально **вызывает** `plan.load_plan()`, `plan.summary()`, `plan.items`, `plan.to_dict()` → удаление `plan.py` на этапе 2 ломает отчёт (и 4 теста в `test_report.py`). Требует решения (вопрос 3) |
| `ui/state.py`                            | плана **не касается** (только журнал/клиенты/сессия/загрузчики) → переписывать его не нужно, только точечные добавки |
| `glossary.py`                            | подписи экранов живут там; `tests/unit/test_glossary.py` механически проверяет `glossary.nav_label("<key>")` в `app.py` → переключение на SCR-подписи затрагивает глоссарий и этот тест |
| Старый UI / тесты                        | `ui/pages/**` 11 файлов, `ui/common/**` 7 файлов; за `plan*` держатся `test_plan.py` (411 стр.), `test_plan_panel.py` (241), `test_plan_runner.py` (384), `test_report.py`, `test_checks_page.py`, `test_console_page.py`, `test_exchange_monitor.py`, `test_api_notes.py`, `test_ui_labels.py`, `test_stand_snapshot.py` (импорт `ui.common.labels`), `tests/smoke/test_app_smoke.py` (1457 стр., 60 тестов, из них `test_every_registered_screen_renders` — прообраз гейта «15 экранов») |

---

## План этапа 2 (черновик `llm_tasks/23.3. План этапа 2 (каркас экранов, удаление старого UI).md`)

### 1. Цель и границы

**Входит:** новый каркас интерфейса (реестр 15 экранов, 5 групп навигации, шапка, ресурсы) + 15 скелетов + **единовременное удаление** старого UI, `plan.py`, `plan_runner.py` и legacy-поля `plan`.

**Не входит:** наполнение экранов данными (этап 3), `docs/12` и `test_traceability.py` (этап 4), `report.py` «Программа и объём» и справка `docs/17`/`help/` (этап 5), `mockup/**` (не трогаем — он и `docs/16` источник истины для подписей).

### 2. Реестр экранов (`acceptance/ui/nav.py`) — как в `docs/16` и `mockup/`

| Группа (`IR-P-1`)      | Экраны (код · ключ · подпись · фаза)                         |
| ---------------------- | ------------------------------------------------------------ |
| Планирование испытаний | `SCR-14` · `scr14_sets` · 🧭 Наборы проверок · Ф0 · `SCR-15` · `scr15_programme` · 🎯 Программа сессии · Ф0, Ф3 |
| Подготовка             | `SCR-01` · `scr01_overview` · 📊 Обзор испытаний; `SCR-02` · `scr02_stand` · 🖥️ Стенд; `SCR-03` · `scr03_session` · 🧪 Сессия испытаний; `SCR-04` · `scr04_data` · 🗃️ Данные стенда |
| Испытания              | `SCR-05` · `scr05_run` · ▶ Прогон; `SCR-06` · `scr06_check` · 🔎 Карточка проверки; `SCR-07` · `scr07_tasks` · ⏱️ `$Задачи` (**последний** — как требует `test_target_docs`) |
| Результаты             | `SCR-08` · `scr08_protocol` · 📋 Протокол; `SCR-09` · `scr09_journal` · 🧾 Журнал обмена; `SCR-10` · `scr10_notes` · ✍️ Замечания к API; `SCR-11` · `scr11_report` · 📄 Отчёт испытаний; `SCR-12` · `scr12_compare` · 🔁 Сравнение сессий |
| Инструменты            | `SCR-13` · `scr13_tools` · 🛠️ Консоль и настройки             |

В `nav.py` — код, ключ, группа, фаза, `DEFAULT_SCREEN`, `screens()`, `by_code()`, `group_of()`, `group_keys()`, `validate()` (сохраняем как есть — тест-«замок» уже это требует); **подписи — из `glossary.py`** (расширяем `SCREEN_LABELS`/`SCREEN_ICONS` на 15 целевых экранов, «$Задачи» остаётся с префиксом).

### 3. Артефакты

| Действие   | Файлы                                                        |
| ---------- | ------------------------------------------------------------ |
| Переписать | `acceptance/ui/nav.py`, `acceptance/app.py`, `tests/unit/test_nav.py`, `tests/smoke/test_app_smoke.py`, `acceptance/glossary.py` (подписи/иконки экранов) |
| Создать    | `acceptance/ui/screens/__init__.py` + `scr01_overview.py … scr15_programme.py` (15 скелетов), `acceptance/ui/components/**` (перенос годного) |
| Точечно    | `acceptance/ui/state.py` (0–2 добавки: кэш каталога проверок / доступ к программе и очереди), `acceptance/report.py` + `tests/unit/test_report.py` (отвязка от `plan`) |
| Удалить    | `acceptance/plan.py`, `acceptance/plan_runner.py`, `acceptance/ui/pages/**` (11 файлов), `ui/common/plan_panel.py`, `ui/common/check_run.py`, legacy-поле `plan` и его хелперы в `session.py`, `tests/unit/test_plan*.py`, `test_checks_page.py`, `test_console_page.py` |

### 4. Порядок работ (два коммита, репозиторий зелёный между ними)

1. **Коммит 2a «каркас»** — новый `nav.py`, 15 скелетов, новый `app.py` (шапка: адрес/сессия/сборка/ревизия программы/уровень журнала, 5 групп, `nav.validate`), подписи в `glossary.py`, новый `test_nav.py`, новый смоук «15 экранов открываются + `pult_started` пишется в журнал». Старый UI ещё лежит на диске, но `app.py` на него не ссылается; старые unit-тесты страниц пока зелёные.
2. **Коммит 2b «удаление»** — перенос переиспользуемого в `ui/components/**` + удаление `plan*.py`, `ui/pages/**`, `plan_panel.py`, `check_run.py`, legacy-поля `plan`; отвязка `report.py` от `plan`; удаление/переписывание тестов старого UI; гейты целиком.
3. **Коммит 2c** — `llm_tasks/23.3. Что сделано.md` с фактическими результатами гейтов + push.

### 5. Гейты этапа 2

`pytest -q` · `ruff check .` · `ruff format --check .` · `mypy acceptance tests` · `tools.gen_endpoints --check` · `node --check mockup/*.js` · смоук «15 экранов открываются» · вручную `streamlit run acceptance/app.py`: все 15 экранов и 5 групп.

⚠️ **Оговорка по гейту из `.clinerules`:** «живой вертикальный срез набор → программа → утверждение → Следующая → протокол» на этапе 2 пройти нельзя — экраны пустые (срез закрывается на этапе 3, начиная с `SCR-15`); сейчас срез проходит вручную на ядре (сделано в 1b).

---

| №    | Вопрос                                                       | Варианты                                                     | Моя рекомендация                                             | Ответ                                                        |
| ---- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 1    | Главный: пульт между этапом 2 и 3 (после удаления старого UI остаются только скелеты) | (а) как в плане: удалить старое сразу, пульт до этапа 3 показывает скелеты; (б) старое UI сохранить «архивом» (скрытая страница/флаг) до конца этапа 3 и удалить вместе с первыми экранами; (в) слить этапы 2 и 3 в один (каркас + все экраны сразу, удаление в конце) | **(б)** — вы сохраняете возможность демонстрации/работы на старом пульте, пока новые экраны не готовы; вариант (а) — если демонстрации не нужны и важен чистый репозиторий | а                                                            |
| 2    | Что показывает скелет экрана на этапе 2                      | (а) минимум: код · название · группа · фаза · назначение · требования `docs/16` · бейдж «в разработке (этап 3)»; (б) плюс заготовка трёх зон (навигация/рабочая область/панель контекста) и состояния «нет сессии/ошибка»; (в) сразу подключать данные там, где ядро готово (`SCR-14`, `SCR-15`, `SCR-05`) | **(б)** — каркас трёх зон сразу, иначе `SCR-05`/`SCR-06` на этапе 3 придётся переделывать, а вклад «как в макете» не виден | б                                                            |
| 3    | `report.py` держится на `plan.py` (удаляем на этапе 2)       | (а) перевести раздел плана отчёта на `programme` + `queue` + `results`, `plan.csv` → `programme.csv`; (б) временно убрать раздел плана/приложение/`payload["plan"]` из отчёта до этапа 5; (в) отложить удаление `plan.py` и поля `plan` до этапа 5 (тогда legacy-поле живёт ещё два этапа) | **(а)** — отчёт не беднеет, `plan.py` уходит полностью; объём ~100 строк в `report.py` + 4 теста | б                                                            |
| 4    | Переиспользуемые компоненты старого UI: `exchange_monitor` (лента обмена), `run_card` (карточка запуска), `api_notes`, `labels`, `pagination`, `flash` | (а) перенести в `acceptance/ui/components/**` и адаптировать (лента → `components/exchange_feed.py`, карточка запуска → на новом `runner`); (б) выбросить всё и писать заново на этапе 3 | **(а)** — ≈800 строк проверенного кода (есть тесты) переезжают, а не выбрасываются; `check_run.py` удаляем (его роль занял `runner.py`) | По сути здесь вопрос веры - получится ли у тебя адаптировать старый код без ошибок или нет. Как разработчик я бы набросал каркас заново, а потом наполнял функционал подглядывая и копируя куски старого кода. Если у тебя есть статистика  использования LLM обоих подходов, опирайся на нее, выбери что будет содержать меньше ошибок. |
| 5    | Тесты старого UI                                             | (а) удалить тесты удалённых модулей (`test_plan*`, `test_checks_page`, `test_console_page`), тесты перенесённых компонентов сохранить с правкой импортов, смоук переписать под 15 экранов; (б) сохранить всё как «архив» в `tests/legacy/` | **(а)** — архив мёртвых тестов будет врать о состоянии кода  | а                                                            |
| 6    | Стартовый экран пульта                                       | (а) `SCR-01` «Обзор испытаний» (маршрут оператора начинается с «где я»); (б) `SCR-14` «Наборы проверок» (как в прототипе — первый экран первой группы) | **(а)**                                                      | По сути тут вопрос о том, как чаще будет использоваться пульт. Если сделать центральной ролью инженера-испытателя, то SCR-01, а SCR-14 помещаем в конец меню. Но если для нас не составит труда впоследствии поменять очередность экранов, то на первое время, все же, я бы поставил то, что сейчас называется SCR-14 вперед.<br />Кстати, категорически нужно перенумеровать их, так чтобы все шло по порядку. И я бы ввел какой-то префикс или индекс для разных разделов. Например, проектирование начинается с номера 101, следующий раздел с номера 201. Или 1-01, ..... 2-01, ..... |
| 7    | 4 уточнения этапа 1b (`llm_tasks/23.2. Что сделано.md` §4): карточка запуска и в «по вызовам»; шаг без параметров не уходит и не «съедает» шаг; «Пачка tech» останавливается на первом не-`tech` пункте (в прототипе перепрыгивала); параметры вызова хранятся в `QueueItem.params` | (а) подтверждаю все 4 как есть; (б) подтверждаю, кроме №3 — вернуть поведение прототипа (перепрыгивать не-`tech`) | **(а)**                                                      | а                                                            |

Ответьте, пожалуйста, прямо в столбце «Ответ» (можно скопировать таблицу в md-редактор). После этого переключите на **Act mode** — я создам `llm_tasks/23.3. План этапа 2 …md`, зафиксирую ваши ответы в нём и начну с коммита 2a «каркас».