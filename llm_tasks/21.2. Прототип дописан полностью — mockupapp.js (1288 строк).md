Этап завершён. Итог по всем шагам плана `llm_tasks/21`:

## 1. Прототип дописан полностью — `mockup/app.js` (1288 строк)

Добавлены недостающие части:

| Блок                                                         | Что добавлено                                                |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| `SCR-05 Прогон`                                              | `runCardHtml` (карточка запуска как гейт для `live`/`heavy`), `controlsHtml` (один «▶ Пуск», «▶▶ Пачка tech», «⏯ Авто/⏸ Стоп», пауза, KPI), `renderRun` (очередь + управление + лента с **независимой** подробностью запроса и ответа) |
| `SCR-06 Карточка проверки`                                   | шаги, payload/preview, **лента внутри карточки** с диапазоном журнала, доказательства; пуск только ссылкой на SCR-05 |
| `SCR-07 $Задачи`                                             | вкладки Наблюдение / Список / Архив / Диагностика, доступные команды, история FSM-1, celery-test с подтверждением |
| `SCR-08 Протокол`                                            | фильтры, таблица с причинами снятия, KPI выборки, **ручная отметка с обязательным заключением**, ни одной кнопки «Пуск» |
| `SCR-09 Журнал`, `SCR-10 Замечания`, `SCR-11 Отчёт`, `SCR-12 Сравнение`, `SCR-13 Инструменты` | реестр/детали обмена, замечания + перспективные требования + регресс, готовность отчёта и решение, дельта сессий/сборок, консоль/диагностика/настройки/журналы/справка/словарь |
| Движок прогона                                               | `runItem` (пошаговая симуляция обменов с вердиктами), `runNext`, `runBatch`, `startAuto`/`stopAuto` (остановка на пункте, требующем оператора) |
| События                                                      | делегирование `data-act` (45 действий), `data-set` (селекты/поля), горячие клавиши (`Ctrl+Enter` — пуск, `Esc` — закрыть/стоп, `Alt+1…9` — экраны), `hashchange`, переключатель состояний UI |

## 2. Проверка прототипа

* `node --check mockup/app.js` и `demo-data.js` — синтаксис валиден (в процессе нашёл и исправил опечатку кавычки в `renderReport`).
* Headless-прогон в Node с заглушками DOM: **13/13 экранов отрисованы**, «Пуск» добавляет обмены в ленту и обновляет счётчики, протокол показывает строки, ручная отметка без заключения отклоняется, снимок «Окончание» работает.

## 3. Стоп-тесты `tests/unit/test_target_docs.py` (14 тестов)

Непрерывность нумерации `FR-P/DR-P/IR-P/AR-P/NFR-P/AC-P` (1..64, 1..12, 1..16, 1..10, 1..12, 1..24); совпадение перечня `SCR-01…SCR-13` в ТЗ, макете **и** реестре прототипа; у каждого экрана макета — требования, назначение, состояния; все ссылки макета разрешаются в ТЗ; прототип офлайн (нет CDN/сборки/`fetch`); ТЗ, макет и `mockup/` зарегистрированы в `docs/README.md` и `README.md`.

## 4. Регистрация и отчёт

* `docs/README.md` — строки для `docs/15` и `docs/16`;
* `README.md` — ссылки на `docs/15`, `docs/16`, `mockup/`, `mockup/` в дереве структуры, число тестов 693;
* `llm_tasks/21.1. Что сделано (целевое ТЗ, макет, прототип).md` — отчёт этапа (решения, возможности прототипа, стоп-тесты, гейты, ограничения, следующий шаг).

## 5. Гейты (все зелёные)

| Гейт                                         | Результат                                    |
| -------------------------------------------- | -------------------------------------------- |
| `python -m pytest`                           | **693 passed, 3 skipped** (было 679)         |
| `python -m ruff check .`                     | All checks passed                            |
| `python -m ruff format --check .`            | 189 files already formatted                  |
| `python -m mypy client lib acceptance tests` | Success: no issues found in 126 source files |
| `python -m tools.gen_endpoints --check`      | реестр 40 операций актуален                  |

Итого «идеальное состояние» пульта зафиксировано в трёх независимых от кода формах: `docs/15` (481 строка, 148 требований), `docs/16` (582 строки, 13 экранов), `mockup/` (1620 строк, открывается двойным щелчком офлайн) — и защищено стоп-тестами от расхождения частей. Код `acceptance/**` в этом этапе не менялся; следующий шаг — аудит соответствия существующего кода этому эталону (план M2–M5).