Этап 2 закрыт, всё отправлено в `origin/master`. Итог:

## Что сделано (4 коммита + отчёт)

| Коммит    | Шаг     | Содержание                                                   |
| --------- | ------- | ------------------------------------------------------------ |
| `4f1f2f1` | **2.0** | перенумерация экранов `SCR-NN` → `SCR-<раздел><порядок>` (344 строки в 17 файлах) |
| `f4ebd38` | **2a**  | каркас: реестр навигации, 15 скелетов, новый `app.py`, подписи в глоссарии |
| `c9a2845` | **2b**  | удаление старого UI, `plan*.py`, поля `plan`, раздела плана в отчёте |
| `c2be3e6` | —       | уточнение счётчиков в отчёте `llm_tasks/23.3. Что сделано.md` |

**Перенумерация:** `SCR-101/102` (планирование) → `SCR-201…204` (подготовка) → `SCR-301…303` (испытания) → `SCR-401…405` (результаты) → `SCR-501` (инструменты). Правки в `docs/12/14/15/16`, `docs/README.md`, `README.md`, `.clinerules`, `mockup/**`, докстрингах ядра и тестах; `docs/15`/`docs/16` — версия 1.2, в ТЗ §1.5 добавлено правило нумерации экранов. История (`llm_tasks/**`, строки «История документа») не переписывалась — реестр соответствия в `llm_tasks/23.3`.

**Каркас:** `acceptance/ui/nav.py` (15 экранов: код, ключ маршрута, группа, фаза; `validate()`), `acceptance/ui/screens/scr101_sets.py … scr501_tools.py` (15 скелетов с содержимым из `docs/16`: назначение, блоки, состояния, переходы, требования), `acceptance/ui/components/screen.py` (трёхзонная раскладка «рабочая область + панель контекста» — ваш ответ 2б), новый `acceptance/app.py` (шапка: стенд, сессия, сборка, ревизия программы, уровень журнала; пять групп; события `pult_started`/`app_run` с кодом экрана). Стартовый экран — «Наборы проверок» (ваш ответ 6), смена позже = одна строка `DEFAULT_SCREEN`.

**Удалено:** `plan.py`, `plan_runner.py`, `ui/pages/**` (11 экранов), `ui/common/**` (7 модулей), поле `plan` схемы v6, раздел 12 отчёта + `plan.csv` + `payload["plan"]` + `readiness["plan"]` (ваш ответ 3б), 7 файлов тестов старого UI. Чистые подписи (`build_label`, `short_id`, `head_text`) переехали в ядро — `acceptance/labels.py`.

**По вопросу 4** выбрал «писать заново, подглядывая»: старые компоненты были завязаны на удалённое ядро (`plan`, `plan_runner`, `session.plan`), порт дал бы ошибки только в рантайме; образцы остаются доступны через `git show pre-bigbang:acceptance/ui/common/<файл>` — их переносим на этапе 3 под `queue`/`runner`/`results`.

## Гейты (фактические, после 2b)

```
python -m pytest -q                     → 640 passed, 3 skipped
python -m ruff check .                  → All checks passed!
python -m ruff format --check .         → 204 files already formatted
python -m mypy acceptance tests         → Success: no issues found in 105 source files
python -m tools.gen_endpoints --check   → реестр актуален: 40 операций
node --check mockup/app.js demo-data.js → ok
смоук AppTest «15 экранов»              → 7 passed (все 15 открываются, 5 групп, 15 кнопок, журнал)
```

`git status` чист, `master` == `origin/master` (`c2be3e6`).

## Что дальше и на что обратить внимание

1. **Этап 3** — наполнение экранов в порядке вертикального среза: `SCR-102` → `SCR-101` → `SCR-301` → `SCR-302` → `SCR-401` → остальные; тогда же закрывается «живой вертикальный срез» из `.clinerules` (сейчас его пройти нельзя — экраны пустые).
2. Пока этап 3 не начат, **пульт показывает скелеты без функционала** (это следствие вашего ответа 1а); рабочая версия — ветка `stable` (тег `pre-bigbang`).
3. Документация честно отстаёт: `docs/12` (РЭ v1) и `BACKPORT.md` ещё описывают прежний UI — по плану это этап 4; отметил это в отчёте §7.