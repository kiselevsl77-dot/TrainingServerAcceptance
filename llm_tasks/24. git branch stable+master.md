Готово. Сделал ровно то, что просили.

## Что сделано

**1. `stable` — замороженная ветка (то, что было закоммичено и опубликовано)**
```
git branch stable 04bd1a6      # local stable → 04bd1a6, master не двигался
git push -u origin stable      # new branch  stable -> stable  (tracking origin/stable)
```

**2. Якорь версии — тег `pre-bigbang`** (по Q14 плана 23; тот же коммит `04bd1a6`)
```
git tag pre-bigbang 04bd1a6
git push origin pre-bigbang    # new tag  pre-bigbang -> pre-bigbang
```

**3. `master` — ветка разработки big bang** (в ней же остались незакоммиченные файлы этапа 1a, ничего не переключалось)
| Коммит    | Содержание                                                   |
| --------- | ------------------------------------------------------------ |
| `03bf975` | **этап 1a**: `acceptance/sets.py` (738), `acceptance/programme.py` (897), схема сессии v7, тесты `test_sets` (19) + `test_programme` (27), план `llm_tasks/23…` и отчёт `23.1` (9 файлов, +2783 строки); в теле коммита зафиксированы результаты гейтов |
| `9b47921` | `.clinerules`: раздел «Ветки и выпуск» + исправлено устаревшее упоминание пакета `acceptance/core/**` → модули плоско (решение §3.1 плана 23) |

`git push origin master` → `04bd1a6..9b47921 master -> master`.

## Фактическое состояние после работы

```
refs/heads/master      9b479214481cd4d395d42ba13100859ff58b1080   ← разработка (big bang)
refs/heads/stable      04bd1a602c3f5ec5ee70526de3205b7a8ecae9aa   ← заморожено «перед big bang»
refs/tags/pre-bigbang  04bd1a602c3f5ec5ee70526de3205b7a8ecae9aa
git status --porcelain → пусто (все изменения закоммичены)
```

**Гейты на закоммиченном `master` (прогнал заново):**
```
python -m pytest -q                  → 744 passed, 3 skipped (80 с)
python -m ruff check .               → All checks passed!
python -m ruff format --check .      → 200 files already formatted
python -m mypy acceptance tests      → Success: no issues found in 110 source files
```

## Дальнейший режим работы (записан в `.clinerules`, раздел «Ветки и выпуск»)
* `stable` — только аварийные правки старого пульта;
* `master` — этапы 1b → 2…6, каждый этап = коммит с фактическими цифрами гейтов;
* когда big bang пройдёт приёмку: `git checkout stable && git merge --ff-only master && git push`;
* тег `pre-bigbang` не переставляем.

Единственное, что командой git не сделать: назначить `stable` веткой по умолчанию в GitHub — это Settings → Branches → Default branch (клик в веб-интерфейсе). Сейчас по умолчанию `master`, что для разработки удобно.

Готов продолжать: у меня остаются **не отвеченными вопросы 1–5 по этапу 1b** (черновик программы, режим «по вызовам», поведение авто-прогона при отказе, состав «Пачки tech», файл плана `llm_tasks/23.2`) — как только ответите таблицей, начинаю 1b.