Готово. Документ создан, зарегистрирован и проверен; код, контракт и тесты не тронуты.

## Что сделано

**1. `docs/09. Контракт API 17.09.2026 — изменения и план работ.md`** (новый, 414 строк / 36 КБ) — 18 разделов:

| Раздел | Содержание                                                   |
| ------ | ------------------------------------------------------------ |
| §0     | источники и SHA-256 обоих контрактов, метод разбора, команды снятия baseline, термины |
| §1     | сводка **27/32/34/6 → 34/40/49/10**, удалённых операций и схем нет, распределение 40 операций по 10 модулям |
| §2     | 8 новых операций AutoML с параметрами, телами, кодами ответов (201/202/200) и предлагаемыми классами (`heavy` ×2, `destructive`, `read` ×5) |
| §3     | 15 новых схем с обязательными полями и назначением (включая discriminated union правил и `SortKey`) |
| §4     | переименование `ML models` → `initialization`/`training`/`testing`/`inference` (11 операций, только тег) и его следствия |
| §5     | закрытие P0: `ph_n` удалён из `GET /api/loads/list`, `phase_connection` удалён из `LoadDevice`/`UpdateLoadRequest` |
| §6     | `TaskType += find-params` и влияние на фильтр монитора задач |
| §7     | 7 находок (в т.ч. отсутствующий в `paths` `POST /find_params/population`, `text/csv` у `grid.csv`, пустая схема ответа Loads, свободные `object`) с приоритетами |
| §8     | план **К0 → К1 → К2** таблицами «работа / файлы / готово, когда» + правило порядка (сначала коммит T3) |
| §9     | 5 открытых вопросов (имя контракта в репозитории, обратная синхронизация постановки, этап для AutoML, судьба закрытого P0, вендорная копия) |
| §10.1  | **полный список 40 операций**: №, модуль, метод и путь, класс, тело, ответ, все имена параметров |
| §10.2  | **7 обязательных требований независимости AutoML** (отдельная группа/файл, «недоступно в сборке» вместо «отказа», не входит в `tech`-прогон, независимость по данным, в отчёте, в регрессии; реестр консоли — исключение) |
| §10.3  | инвентарь тестов-сторожей: файл → что утверждает → что станет |

**2. `docs/README.md`** — новая строка в перечне документов (описание док. 09) + абзац-фиксация факта: обновлённый контракт получен файлом `docs/api_17_09_26.json` (34/40/49/10 против 27/32/34/6), порядок фиксации — в док. 09 §8–§9.

**3. Проверка:**
- `python -m pytest tests -q` → **477 passed, 3 skipped** (базис не изменился);
- `git status -- docs` → изменён только `docs/README.md`, добавлен `docs/09. …md`; строки таблицы `docs/README.md` не слиты (проверено чтением файла);
- заголовки документа и таблицы проверены чтением (структура §0–§10 целая).

## Ключевые факты для решения (из разбора)

- Изменений «в минус» нет: только +8 операций, +15 схем, переименование тегов, закрытие P0 по Loads.
- **Дефект P0 пульта закрыт не так, как ожидалось**: сервер не начал возвращать `phase_connection`, а перестал требовать фазу (`phase_connection` удалён из `LoadDevice`/`UpdateLoadRequest`) вместе с параметром `ph_n` — поэтому `TC-LOAD-02/03` переформулируются, а не просто «проходят».
- Два места, где пульт сам расходится с новым контрактом и это надо править в К0: `client/schemas.py` отправляет обязательный `phase_connection`, а генератор реестра отдаёт для `grid.csv` вид ответа `json` вместо CSV.

Следующий шаг — по вашему решению из §9 (прежде всего: фиксация контракта как `docs/SOM1.json` + историчная копия, и отдельный этап для AutoML), после чего стартует шаг 0.1 К0. Скажите, когда начинать К0 — и нужно ли предварительно закоммитить незавершённый этап T3 (в рабочем дереве 20 изменённых и 12 новых файлов).