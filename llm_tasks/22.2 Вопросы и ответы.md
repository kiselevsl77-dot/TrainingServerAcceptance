Понял и зафиксировал правило. Ниже — все открытые вопросы в виде таблицы с пустым столбцом **«Ответ»**: скопируйте её в md-редактор, заполните последний столбец — по этим ответам я соберу план этапа. Интерактивных виджетов в дальнейшем не использую.

## Правило работы (на будущее)
* Вопросы задаю **только таблицей** со свободным столбцом «Ответ» — без всплывающих вариантов.
* В plan-режиме ничего не пишу и не меняю, только читаю и анализирую.
* Решения фиксирую в `llm_tasks/` (план этапа → отчёт «что сделано»), чтобы диалог оставался восстановимым по md-файлам.

В корне репозитория сейчас **нет** файла постоянных правил (`.clinerules`, `AGENTS.md`, `CONTRIBUTING.md` отсутствуют) — см. вопрос Q10.

## Открытые вопросы

| №    | Вопрос                                                       | Варианты                                                     | Что зависит от ответа                                        | Ответ                                                        |
| ---- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Q1   | Какой вариант переработки приложения выбираем основным       | **A** лоскутная надстройка (новые экраны рядом со старыми) · **B** data-first: ядро → экраны · **C** big-bang (15 экранов + схема v7 разом) · **D** параллельный клиент · **E** переход за флагом (обычно вместе с B) | Состав этапов, объём переписываемых тестов, порядок работ    | предпочитаю big bang, чтобы не тянуть "хвосты". Полагаю, что с опорой на имеющийся код можно и вовсе построить все заново. |
| Q2   | Режим «по вызовам» (`plan.MODE_CALL`) сохраняем как режим исполнения? | 1) да, оставляем (в `SCR-05` он есть в макете) · 2) нет, только «проверка целиком» · 3) оставить, но спрятать до востребования | Живёт ли двухуровневый `plan.py` (584 строки) или сокращается почти вдвое | Не понял вопроса. В SCR-05 меня пока все устраивает.         |
| Q3   | Что делать со старыми сессиями (схема v6) при переходе на v7 | 1) миграция: старый план конвертируется в «набор + программа» · 2) только новые сессии, старые читаются как есть · 3) миграция + пометка «программа восстановлена автоматически» | Нужен ли модуль миграции и тест на реальном файле v6 из `acceptance_data/` | Если речь о сохранении выполненных сессиях, то никаких ценных данных пока нет, можно "убивать". |
| Q4   | Роли («Руководитель испытаний» / «Инженер-испытатель»)       | 1) полноценный RBAC (пользователи, права) · 2) «режим роли»: переключатель + запись в сессию + запрет запуска для руководителя · 3) пока только в ТЗ/макете, в коде позже | Нужны ли изменения схемы сессии и прав в UI уже в этом цикле | Нет, пока можно в код не заносить. Мои комментарии на этот счет появились только в связи с вопросами о ролях. Но можно внести это в документацию как задел на будущее. |
| Q5   | «Пачка tech» и «Авто с паузой»                               | 1) сохраняем как есть (в ТЗ они есть) · 2) переводим в «Следующая с повтором» (одна команда + параметр повтора) · 3) убираем из первой версии | Насколько близко `SCR-05` ложится на текущий `plan_panel.py` | Сохраняем                                                    |
| Q6   | Модели и AutoML (этапы T6–T11, ~21 проверка)                 | 1) входят в этот цикл (вкладка `$Модели` в `SCR-04`, группы в каталоге) · 2) после M2b · 3) отдельным этапом «T6+» | Появится ли вкладка моделей в `SCR-04` и группы `TC-MOD/TR/INF/AUTO` в справочнике | Можно отложить на следующие этапы                            |
| Q7   | Справочник-энциклопедия и `FR-P-71…73` (вы выбрали «энциклопедия + html-версия руководства») | 1) параллельный поток, начинаем сразу · 2) после M2b · 3) после всего цикла переработки | Порядок этапов, попадает ли `docs/17` + `help/` в ближайший этап | Полагаю, что будет правильным наполнить энциклопедию первым разделом (система) для тестирования интерфейса, а заполнить документ полностью после разработки кода, по факту реализации. |
| Q8   | Объём ближайшего этапа по справочнику                        | 1) `docs/17` + генератор + `help/HTML` + перенос прозы из §18–§22 + требования в `docs/14–16` + прототип · 2) то же **плюс** панель «?» и вкладка «Справочник» в текущем пульте · 3) только `docs/17` + генератор + HTML (посмотреть, потом продолжить) | Что делать в ближайшем этапе и что отложить                  | Сейчас думаем о big bang, а не об эксплуатационной документации.<br />Если же речь о требованиях, то возможно будет не лишним его переписать / обновить. |
| Q9   | Что делаем **первым шагом**                                  | 1) M2a — ядро планирования (наборы, программа, ревизии, покрытие, единый результат) · 2) M2b — экраны (навигация 5 групп, `SCR-14/15/05/06/08/01`) поверх текущего ядра · 3) M4 — справочник · 4) сначала «мостик»: требования в `docs/14–16` довести и синхронизировать `docs/12` | Первый набор изменений и критерий готовности этапа           | Ядро                                                         |
| Q10  | Где зафиксировать правило ведения диалога (вопросы таблицей, ответы в md) | 1) `.clinerules` в корне (машинное правило для агента) · 2) раздел в `README.md` · 3) `llm_tasks/00. Правила работы с заказчиком.md` · 4) ничего не создавать — держать в текущем диалоге · 5) комбинация, например 1+3 | Появится ли новый файл правил в этапе и попадёт ли он в описание структуры репозитория | .clinerules                                                  |

## Что ещё стоит решить перед стартом (если ответите «да» — включу в план)
1. **`docs/12` синхронно с кодом**: при переработке экранов руководство правим в том же этапе (иначе оператор читает устаревшее) — или честно помечаем разделы «описывает состояние до M2».
2. **Тест трассировки «код ↔ ТЗ»** (`docs/15`/`docs/16` → модули/экраны, с явными пометками «не реализовано»): включаем в гейты этапа? Без него разрыв между кодом и ТЗ будет виден только на глаз.
3. **Окно для живого прогона**: есть ли ограничение по дате/стенду, которое диктует, когда можно ломать текущее поведение (влияет на выбор B против C и на необходимость флага E).

Как только пришлёте заполненную таблицу, соберу по этим ответам `llm_tasks/23. Утверждённый план этапа` с шагами, файлами, тестами и гейтами — и после переключения в Act mode начну по нему.