Разобрал логи `apptestslog/26-09-17/` (отчёт md+json и сессия `20260917-133033-8e66.jsonl`) и дополнил их **живыми read-only пробами стенда** — стенд доступен из этого окружения, авторизации нет, так что часть проверок я выполнил сам.

---

# 1. Что показал лог сессии 17.09

| Показатель     | Значение                                                     |
| -------------- | ------------------------------------------------------------ |
| Сессия / отчёт | `20260917-133033-8e66`, `report_20260917-133033-8e66.md/.json` (41 проверка в каталоге) |
| Итог           | 30 из 41 выполнено: **успех 26**, **отказ 1**, **блокировано API 3**, не выполнено 9, пропущено 2 |
| Группы         | TC-SYS 5/6 (1 отказ), TC-FILE 11/14 (1 blocked, 3 не запускались), TC-REC 5/6, TC-LOAD 7/7 (2 blocked), TC-TASK 2/8 (4 не запускались) |
| Замечания      | 2, оба P0, оба созданы автоматически                         |
| Полнота        | **снимков стенда «начало»/«окончание» нет**, наблюдаемых задач 0, тестовых сущностей 0, артефактов 2 (снимок чек-листа) |
| Живые данные   | 265 файлов (RAW 129 / LOADS 129 / H5 5 / REPORT_ZIP 2), 39 ГБ, 129 записей, 62 нагрузки, 11 задач, сборка **dev@58ac72f** |

**Отказ `TC-SYS-03`** — «неизвестный путь отвечает 405 вместо 404». Причина подтверждена мной живой пробой: `GET /api/__test_unknown__` → `405` с `allow: DELETE` (корневой маршрут `DELETE /api/{file_id}` перехватывает `/api/{file_id}`). Это не дефект пульта, а живое подтверждение замечания «коллизии в корне `/api/`» (уже зафиксировано в `docs/08`).

**Что в этом отчёте уже устарело после К0** (важно для следующего прогона):
- `TC-LOAD-02`/`TC-LOAD-03` «блокировано API» и замечание P0 «нет `phase_connection`, `ph_n` не фильтрует» — это **до-K0 состояние**: контракт 17.09 убрал фазу и параметр, а сценарии переписаны. Новая логика по пробам (§2) даст «успех» с зафиксированным фактом «сервер игнорирует неизвестный параметр».
- Предупреждение отчёта «статус „блокировано API“ без замечания: TC-LOAD-03» — прямое следствие: шаблон дефекта для Loads снят, а в самой сессии остался прежний статус BLOCKED. Для исторических сессий это ожидаемо, но это надо явно описать в `docs/10` (иначе выглядит как дефект отчёта).

# 2. Живые пробы, которые я выполнил сейчас (только GET, ничего не менялось)

| Проба                                                        | Ответ                                                        | Что это значит для плана                                     |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `GET /version`                                               | `dev@83319ae`, build **2026-09-17T15:08**                    | сборка **новее**, чем в сессии 17.09 (`dev@58ac72f`) — то есть контракт 17.09.2026 соответствует именно этой сборке; в шапке отчёта сборку фиксируем заново |
| `GET /api/ml_models/find_params/population`                  | **200 `[]`**                                                 | **блок AutoML в сборке есть**; ветка «недоступно в сборке» пока не наш случай → живой happy-path в T11 реален |
| `GET .../population/{uuid}` и `.../grid.csv`, `.../mutated_models`, `.../selections` | 404 `{"detail":"Population … not found"}`                    | единый формат ошибок для AutoML; `grid.csv` на несуществующей популяции отдаёт JSON, а не 404-страницу (учтём в сценарии T11) |
| `POST /api/ml_models/find_params/population` (одним вызовом) | **в контракте отсутствует**, при этом `GET` на этом пути обслуживается | находка §7.1 `docs/09` подтверждена живой сборкой            |
| `GET /api/datasets/`                                         | 6 (skfu-train, leti-train, skfu-test, leti-test, synt-train, synt-test), все `direct_fill` | для T5 есть данные; для T11 есть заполненный датасет под итерацию отбора |
| `GET /api/ml_models/models`                                  | 6 моделей, все `stft_mlp`/`single`, **`signals = null`**     | замечание P2 «`signals` пусты» живо; для T6 и T11 (создание популяции от модели) есть материал |
| `GET /api/ml_models/architectures`                           | 200, есть `cnn_1d` c `default_config`/`suitable_for_phases`  | T6-01 выполним                                               |
| `GET /api/loads/list?limit=1`                                | `result_size=62`, поля элемента ровно `{load_id, category, description}` — **`phase_connection` нет** | TC-LOAD-02 после К0 → «успех», состав сверяется с контрактом |
| `GET /api/loads/list?limit=1&ph_n=Ph_A`                      | 200, **тот же** `result_size` и тот же первый `load_id`      | сервер **игнорирует** неизвестный параметр → TC-LOAD-03 после К0 = «успех с зафиксированным фактом», без замечания |
| `?limit=1&offset=0` vs `offset=1`                            | `offset` возвращается, выдачи разные (`__OTHER__` → `3Дпринтер`) | срез работает; первая нагрузка — служебная `__OTHER__` (подтверждает замечание №2) |
| `GET /api/tasks/?task_type=find-params`                      | 200 `{"tasks":[],"count":0}`                                 | новый тип задачи принимается сервером (задач пока нет — ожидаемо) |

# 3. Нужны ли отдельные проверки — да, вот минимальный необходимый список

**(а) Только через UI пульта (я не могу заменить клик оператора):**
1. **Снимки стенда** «начало» и «окончание» — без них отчёт снова будет «неполон» (FR-T1, TC-CLEAN-03).
2. **Повторить `TC-LOAD-02` и `TC-LOAD-03`** после правок К0 — чтобы в новой сессии были актуальные статусы вместо до-K0 «блокировано API».
3. **Ручные отметки**: `TC-SYS-06`, `TC-REC-03`, `TC-TASK-08` (с доказательством, для TASK-08 — вердикт строит `tasks.external_observation`).
4. **Live-проверки файлов** `TC-FILE-11/12/13` (создание/загрузка/удаление `__TEST__`-файла) — они ни разу не запускались, а именно они закрывают NFR-T4 (самоочистка) и дают первую запись в `test_entities`.
5. **`TC-TASK-01/04/05`** с выбранным `task_id` (или после `POST /api/tasks/test?duration=1`) — путь FSM-1 в этой сессии вообще не проверялся.

**(б) Что могу сделать сам в Act-режиме без UI:** прогнать `pytest tests/integration/test_live_read.py -m integration` против `dev@83319ae` (только чтение) — это «сухие» машинные доказательства для `docs/10` в дополнение к UI-сессии, плюс повторяемые read-only пробы контракта.

**(в) Сознательно отложено (не сейчас):** `POST`/`DELETE` AutoML (создание популяции), обучение/инференс — это T7/T8/T11 с карточкой запуска, ответственным и расходом ресурсов. Единственная «проба с риском записи» — `POST /api/ml_models/find_params/population` (проверка отсутствующей операции); её стоит делать только в T11 и с контролем, что список популяций не изменился (сейчас он `[]`).

# 4. Три уточнения к плану, которые дал этот прогон

1. **К2-минимум** дополняется: (i) снимки стенда обязательны, (ii) `docs/10` фиксирует **смену сборки** `dev@58ac72f → dev@83319ae` как «объект испытаний», (iii) в `docs/10` — раздел «исторические статусы до-K0» (Loads P0, предупреждение «blocked без замечания» объясняется сменой контракта, а не дефектом).
2. **UX-находка, видимая из логов:** `TC-TASK-03/06` «пропущены», потому что пикер задач предлагает только задачи, **наблюдённые в сессии** (0), хотя на стенде их 11. Иначе проверки TASK/TR/INF будут вечно «пропущены» → в бэклог T5/T9: пикер должен уметь брать задачу из серверного списка (как это уже сделано в консоли `_task_picker`).
3. **Проверка уборки в К2:** после live-проверок файлов должно появиться `test_entities > 0` и `pending = 0` — это заготовка для будущего `TC-CLEAN-01`; в сессии 17.09 сущностей 0 (создающих проверок не запускали).

---

**Итого: отдельных проверок от вас — пять пунктов из (а)** (снимки, повтор Loads, три ручные отметки, live-файлы, задачи). Всё остальное из К2 я могу закрыть сам интеграционными read-only прогонами.

**Порядок дальше не меняю** (обязательное → AutoML в конце): К2-минимум → `docs/10` → T5 (датасеты) → T6 → T7 → T8 → T9 → T10 → **T11 AutoML**. Чтобы я начал править файлы (правки `docs/10`, затем клиент/сценарии T5) и прогнал интеграцию — **переключите в Act mode**; я начну с `docs/10` и машинных read-only доказательств, а пункты (а) вы выполните в UI, после чего допишу отчёт фактами вашей сессии.