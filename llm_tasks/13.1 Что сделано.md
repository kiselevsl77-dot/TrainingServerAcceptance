Инкремент закрыт. Итог по выполненному (Act-режим).

## Что сделано

**1. Живой прогон контракта (шаг К2.1)**
- Прогнал read-only интеграционные тесты против стенда: `python -m pytest tests/integration/test_live_read.py -m integration` → **8 passed** (сборка `dev@83319ae`).
- Разобрал обе сессии: `apptestslog/26-09-17` (до К0) и `apptestslog/26-09-18` (после К0). Ключевое: **К0 подтверждён живой сборкой** — `TC-LOAD-02/03` → «успех» (поля `load_id/category/description`, `ph_n` игнорируется), снимки стенда сняты, замечаний осталось одно (P0 `TC-FILE-10`), итог 28 успех / 1 отказ (`TC-SYS-03`, причина — корневой `DELETE /api/{file_id}` перехватывает `/api/{file_id}`) / 1 блокировано / 2 пропущено.

**2. Отчёт этапа — `docs/10. Живой прогон контракта 17.09.2026 — К2-минимум.md`** (10 разделов, ~14 000 символов): итог, смены сборок `58ac72f → 83319ae`, оба прогона в сравнении, снимки и дельты реестров, консольные вызовы сессии, 12 read-only проб, 7 находок со статусами, остаток из 11 проверок с инструкциями, факты стенда для T5–T11 (6 датасетов и 6 моделей с идентификаторами).

**3. Правки по находкам**
- `acceptance/ui/pages/console.py`: multipart-форма теперь требует `file_type` (пометка `*`, подсказка «без него сервер отвечает 422 `Field required`», отправка блокируется, пока тело неполно) — причина живого 422 из вашей сессии.
- `acceptance/ui/pages/checks.py`: пикер задачи больше не «слепой» — при отсутствии наблюдений показывает живой список задач сервера (`TASK_PICKER_LIMIT = 50`, `_server_task_options`) и перед запуском сценария ставит выбранную задачу на наблюдение (`_ensure_task_observed`: `register_task`/`register_external` + один опрос, `TC-TASK-08` — всегда как внешняя). Это закрывает «пропущены `TC-TASK-03/06` при 14 задачах на стенде».
- Тесты: `tests/unit/test_console_page.py` (+3), **новый** `tests/unit/test_checks_page.py` (12 тестов), плюс попутно исправил **предсуществующую** ошибку mypy в `tests/unit/test_checks_files.py` (аннотация `dict[str, object]` → `dict[str, Any]`).

**4. Документы и статусы**: `docs/README.md` (строка о `docs/10`), `README.md` (живой прогон К2.1 в тексте + этапы: T5 — «следующий», добавлена строка **T11 AutoML**), `docs/09` (статус К2.1 «выполнен»; зафиксировано, что обязательные T5–T10 идут первыми, а К1/К2.2 отнесены к T11).

## Гейты (все зелёные)

| Проверка                                                    | Результат                                        |
| ----------------------------------------------------------- | ------------------------------------------------ |
| `python -m pytest -q`                                       | **496 passed, 3 skipped** (было 483/3)           |
| `pytest tests/integration/test_live_read.py -m integration` | **8 passed** на живом стенде                     |
| `ruff check .` / `ruff format --check .`                    | All checks passed / 146 files already formatted  |
| `mypy client lib acceptance tests`                          | **Success: no issues found in 104 source files** |
| `python -m tools.gen_endpoints --check`                     | «реестр актуален: 40 операций»                   |

Коммит не делал — изменения в рабочем дереве (`docs/10`, `tests/unit/test_checks_page.py` — новые; 8 файлов изменено).

## Что нужно от вас (параллельно)

11 проверок из `docs/10` §7 — коротко: **A)** карточка `TC-FILE-11/12/13` (карточка запуска + «▶ Запустить проверку»); **B)** «Задачи» → «🩺 Диагностика» → `celery-test` 60 с → затем `TC-TASK-01/03` в чек-листе (задача уже подставляется из списка сервера), для `TC-TASK-04/05` — `pause`/`resume` и `interrupt` в карточке задачи перед запуском проверки, `TC-TASK-06` — с выбранной командой; **C)** `TC-TASK-08` через вкладку «🔗 Внешняя задача»; **D)** ручные отметки `TC-SYS-06` (сборки `dev@58ac72f` vs `dev@83319ae`) и `TC-REC-03`; **E)** снимок «окончание» + сохранение комплекта отчёта. Ожидаемый итог — 41 из 41.

## Следующий шаг

По плану — **T5 (датасеты, `TC-DS`, 7 проверок)**: экран датасетов, сценарии (`create/list/meta/update/fill` через монитор, контракт `fill` на несуществующих id, удаление с самоочисткой), пикер `dataset_id` в чек-листе, тесты и `docs/11`. Скажите — и я начну T5 сразу (клиент `client/datasets.py` уже готов, так что работа в основном по экрану и сценариям).