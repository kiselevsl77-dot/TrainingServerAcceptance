Готово. Документ создан, зарегистрирован в обоих README и проверен.

## Что сделано

### 1. Новый документ (26 глав, 2110 строк)

**`docs/12. Руководство пользователя пульта испытаний (функционал и правила).md`**

| Часть                                  | Главы | Содержание                                                   |
| -------------------------------------- | ----- | ------------------------------------------------------------ |
| I. Введение, архитектура, прозрачность | 1–7   | о документе и статусах разделов, термины, назначение и границы, **архитектура** (изоляция, слои, точки расширения), **прозрачность вызовов API**, **оценка результатов работы сервера**, **механизм составления отчётов**, установка/запуск (`.env`, `run_pult.bat`, порт 8502), сквозной порядок работы оператора |
| II. Реализованные разделы (работает)   | 8–17  | **по главе на каждый экран**: Сессия испытаний, Стенд, Записи (RAW + markup), Задачи, Датасеты, Чек-лист проверок, Консоль запросов, Замечания к API, Журнал, Отчёт испытаний — с фактическими подписями вкладок/кнопок и правилами использования |
| III. Проектируемые разделы             | 18–23 | ⚠️ Модели ML (T6), Обучение/ONNX (T7), Инференс (T8), Финализация и уборка (T9), AutoML (T11), Передача комплекта (T10) — каждая глава начинается блоком «**⚠️ Проектируемая часть**», в конце указано, что **уже можно сделать сейчас через консоль** |
| IV. Правила и приложения               | 24–26 | безопасная работа на общем стенде (`__TEST__`, подтверждения по классам, самоочистка), известные дефекты API с обходными путями и живые показатели, приложения (env, структура данных, карта «раздел↔требование», состав каталога, куда смотреть в коде, гейты, сопутствующие документы) |

### 2. Иллюстрации (mermaid) — 10 диаграмм

1. **Архитектура компонентов** (слои UI → Runtime → Apis → LoggingTransport → стенд, журналы и файлы данных) — гл. 3.3
2. **Карта разделов программы и маршрут оператора** (проектируемые экраны пунктиром) — гл. 3.4
3. **sequenceDiagram «путь одного запроса»** (оператор → экран → транспорт → сервер → `Journal` + `app.log` + JSONL) — гл. 4.1
4. **Воспроизводимость: проверка ↔ диапазон журнала** — гл. 4.2
5. **Конвейер оценки результатов сервера** (класс → подтверждение → сценарий → вердикт → сессия/отчёт) — гл. 5.3
6. **Сборка отчёта** (источники → 12 разделов → файлы → артефакты) — гл. 6.3
7. **Жизненный цикл сессии** — гл. 8.5
8. **Правила объединения RAW+markup в записи** (дубли, несопоставленные, ручные решения, манифест) — гл. 10.2
9. **FSM-1** (статусы и `available_actions`) — гл. 11.4
10. **Источники состава датасета** (сервер / факт пульта / гипотеза / недоступен) — гл. 12.2

### 3. Регистрация

* `docs/README.md` — добавлена строка-описание документа `12…` в таблицу документов испытаний.
* `README.md` — новый раздел «## Документация испытаний» со ссылкой на руководство и пояснением про статусы T0–T5 / T6–T11.

### 4. Проверки

| Проверка                          | Результат                                                    |
| --------------------------------- | ------------------------------------------------------------ |
| `python -m pytest -q`             | **539 passed, 3 skipped**                                    |
| `python -m ruff check .`          | All checks passed!                                           |
| `python -m ruff format --check .` | 157 files already formatted                                  |
| Валидация mermaid (скрипт)        | 10 блоков, все закрыты, скобки и кавычки сбалансированы, **нет ссылок на неопределённые узлы** |
| Заголовки                         | 26 глав + 4 части, иерархия 1–26 корректна                   |
| `git status`                      | изменены `README.md`, `docs/README.md`; создан `docs/12. …md` |

Замечание: в документе отсутствуют придуманные сущности — все описания работающей части взяты из кода (`acceptance/**`, `client/**`, `lib/**`), проектируемая часть — из `PLANNED_GROUPS`, `docs/02` §10–§13 и планов этапов; отдельно оговорено, что AutoML/модели/инференс/обучение уже доступны вручную через консоль запросов.