Готово — план реализован полностью, все проверки зелёные.

## Что сделано

**Новый модуль `acceptance/task_snapshot.py`** (снимок статусов + архив)
- `TaskSnapshotEntry` (`status, type, name, start_time, end_time, fetched_at, error, served, archived_at, archived_by`) и `TaskSnapshot`: `observe` (пустое значение не затирает известное), `mark_present` (что сейчас в выдаче сервера), `archive`/`unarchive`/`archive_completed`/`archive_candidates`/`refresh_auto` (авто-архив старше `ARCHIVE_AFTER = 1 сутки`; вернувшаяся в работу задача из архива возвращается), `is_aged`, `archived_entries(include_fresh=…)`, `kpi`, `load_snapshot`/`save_snapshot`, `archive_summary`.
- Файл `acceptance_data/task_snapshot.json` (как `records_overrides.json`); схема сессии осталась **v4**.

**`lib/task_status.py` + `acceptance/tasks_monitor.py`**
- `StatusPresentation` получил `glyph` и `color` (один источник для таблицы, отчёта и легенды): `▶`/`⏸`/`⏳`/`○`/`✔`/`✖`/`⏹`/`?` и green/yellow/red/orange/grey, `UNKNOWN_STATUS`, `presentation_for`.
- Новые `STATUS_GLYPHS/COLORS/ORDER`, `STATUS_ORDER`, `status_glyph/color/text/legend`, `task_card` (проекция карточки), `TaskStatusView` + `task_status_view` (приоритет «карточка → снимок → последний опрос» с указанием источника).

**`acceptance/ui/state.py`**: `load_task_cards` вместо `load_task_statuses` (проекции карточек, параллельно, `STATUS_BATCH = 25`, `STATUS_CACHE_TTL = 30`, `LOAD_TASK_CAP = 500`), `clear_task_cards`.

**`acceptance/ui/common/labels.py`**: `short_id` (…+8 символов), `head_text` (21 символ).
**`acceptance/ui/common/flash.py`**: `repeat_flash` — сообщение оператора не теряется при фоновой перерисовке после батча статусов.

**Экран «Задачи», вкладка «Список сервера»** (`acceptance/ui/pages/tasks.py`): список читается целиком и сортируется локально («создана ↓»), клиентские страницы; колонки `Задача/Тип/Статус/Источник/Наблюдение/Проверка/Название/Описание/Создана/Начало/Окончание/Архив`; статус окрашен через `pandas.Styler` (+`column_config` с подсказками); виды «🗂 Активные | 📦 Архив | Все», флажки «Полные значения» и «Показать перенесённые за сутки»; панель «📦 Архив задач» с правилом, ценой действия, подтверждением и кнопкой переноса; панель строки с полными значениями в `st.code` и кнопками «Наблюдать/Открыть карточку/Открыть в консоли»; батч-догрузка статусов с прогрессом и лог-событиями `tasks_archived[_auto]`/`tasks_unarchived_auto`.

**Проверки и замечания**: TC-TASK-02 получил факты `limit_uncapped`, `list_unsorted`, `list_sort_note`, `archive_is_local`; заметки Task service дополнены (нет `end_time`/признака архива, `limit` не ограничен) — шаблонов по-прежнему 14.

**Тесты**: новые `tests/unit/test_task_snapshot.py` (15), `tests/unit/test_ui_labels.py` (7), расширены `test_tasks_monitor.py` (пиктограммы/цвета/легенда, `task_card`, приоритет источников), `test_checks_tasks.py` (новые факты, обратный порядок, обрезанная выдача), смоук (колонки, сокращение/полные значения, «нет данных» при 500 на карточке, обновление статусов с меткой `TC-TASK-02`, перенос в архив + вид «Архив», герметичный снимок в памяти), живые пробы «список целиком и не отсортирован», «архива в API нет».

**Документы**: `README.md` (экран «Задачи», `task_snapshot.py`, события, `acceptance_data/`), `docs/05` (строка 31), `docs/06` (§5, §6 + два новых подраздела, тесты, §10 + п.12), `docs/07` (новый раздел о доработке, пометка о замене прежнего, обновлённые числа).

**Проверки**: `pytest -q` → **344 passed, 1 skipped**; `pytest -m integration -q` → **16 passed, 1 skipped**; `ruff check`/`format --check` — чисто; `mypy .` — без замечаний (94 файла); `tools.gen_endpoints --check` — реестр актуален (32 операции). Обнаруженный по ходу нюанс: `st.session_state` виджета нельзя менять после создания — сброс подтверждения архива сделан через флаг `tasks_archive_confirm_reset`, а флаш перед `st.rerun` восстанавливается `repeat_flash`.