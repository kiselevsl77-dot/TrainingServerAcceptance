Готово. Пульт приведён в соответствие с контрактом 17.09.2026, контракт зафиксирован как актуальный, всё проверено.

## Что сделано

**1. Контракт зафиксирован как актуальный**
| Файл                        | Содержимое                                       | Размер / SHA-256           |
| --------------------------- | ------------------------------------------------ | -------------------------- |
| `docs/SOM1.json`            | **актуальный контракт 17.09.2026**               | 107 765 Б, `50f8ed29…6b11` |
| `docs/SOM1.2026-08-31.json` | архив предыдущей версии (по ней построены T0–T3) | 69 353 Б, `05dd6e2f…cd06`  |
| `docs/api_17_09_26.json`    | полученная поставка — без изменений              | 107 765 Б                  |

- **`tests/unit/test_contract_version.py`** (новый, 6 тестов) — стоп-тест: размер, SHA-256, состав 34/40/49/10, наличие 8 AutoML-путей и `text/csv`, снятое требование фазы, архивная версия.
- **`tools/sync_docs.py`** — контракт больше **не перезаписывается** синхронизацией (в постановке лежит старая версия 31.08.2026): обновление только флагом `--with-contract`, с выбором свежайшего `api_*.json`/`SOM1.json` и печатью SHA до/после.
- `docs/README.md` — таблица «Версии контракта API» с обеими версиями, составом и контрольными суммами.

**2. Код под контракт**
- `tools/gen_endpoints.py`: новый вид ответа `text/csv` → `RESPONSE_CSV` (`is_csv`/`is_file` в `acceptance/endpoints.py`); `HEAVY_PATHS` += создание популяции и итерация отбора; заметки для всех 8 операций AutoML; переписана заметка Loads.
- **`acceptance/endpoints.py` пересобран: 40 операций, 10 модулей** (AutoML: `heavy`, `heavy`, `destructive`, `read`×5; `grid.csv` → `csv`).
- `client/schemas.py`: `TaskType.FIND_PARAMS = "find-params"`; `phase_connection` **удалён** из `LoadDevice` (был обязательным) и `UpdateLoadRequest`; обновлены замечания `LoadItem`.
- `client/loads.py`: `create_load`/`update_load` без `phase_connection`, докстринги приведены к контракту.

**3. Чек-лист и замечания**
- `TC-LOAD-02` → «Состав полей реестра и контракт», `TC-LOAD-03` → «Неизвестный параметр `ph_n`»: снят `blocked_by_api`, сценарии (`loads.fields`, `loads.unknown_param`) переписаны как фиксация факта (игнор/400/422, 5xx — отказ).
- `acceptance/notes.py`: дефект P0 по Loads переведён в «закрыто контрактом 17.09.2026» (P2, история сохранена), связи `DEFECT_BY_CHECK` для Loads сняты, в `MODULES` добавлен `ML models: AutoML`.

**4. Документация**
- `docs/05`: 40 операций, шкала пересчитана (S1 15 / S2 16 / S3 6 / S4 3), 8 новых строк таблицы с модулями буквально, новый §5.7 (разбор AutoML-тел) и врезка «Изменение от 17.09.2026».
- `docs/01` (ТЗ), `docs/02` (чек-лист: строки TC-LOAD-02/03 + раздел закрытых дефектов), `docs/SOM1. Комментарии.md` (§1.1 состав, §1.2 модули с AutoML, §2.1 п. 7 и 12 — закрытие, новый §7 «Что изменилось и что осталось»), `README.md`, `docs/09` (статус: К0 выполнен, К1/К2 открыты).

**5. Проверка**
```
python -m pytest tests -q   → 483 passed, 3 skipped   (было 477/3)
python -m tools.gen_endpoints --check → реестр актуален: 40 операций
ruff check → All checks passed;  ruff format → 0 изменений;  mypy → 69 файлов, 0 ошибок
```
Итого 24 файла, +2 276 / −588 строк; коммит не делал (оставляю за вами).

## Осталось (по плану `docs/09` §8)
- **К1** — AutoML как независимый блок: pydantic-схемы новых 15 схем + `client/find_params.py`, помощники консоли (`PopulationConfig`, подстановка `population_id`/`model_id`, `grid.csv` артефактом), группа `TC-AUTO` (8 проверок) отдельным модулем — чтобы его доступность не влияла на приёмку остальных модулей.
- **К2** — живой прогон: `/version`, состав полей `loads`, поведение `?ph_n=`, `POST /api/loads` без `phase_connection`, `task_type=find-params`, затем AutoML happy-path на `__TEST__`-популяции.
- **Важно:** правки `docs/01` и `docs/02` — локальные копии постановки; чтобы `python -m tools.sync_docs` не вернул прежние формулировки, их нужно повторить в `TrainingServerUI/context/01. Постановка/05. …` (зафиксировано в `docs/09` и `docs/README.md`).