Данные для приложения собраны полностью (baseline 40 операций из генератора). Ниже — что именно я создам в Act mode, уже без правок кода и контракта.

## Deliverable в Act mode

1. **`docs/09. Контракт API 17.09.2026 — изменения и план работ.md`**
2. **Строка в таблице `docs/README.md`** (регистрация документа — конвенция проекта: `test_request_complexity_doc.py` проверяет регистрацию док 05, для нового документа соблюдаю тот же порядок)
3. Ничего больше: контракт, генератор, реестр, клиент, чек-лист и тесты — не трогаю.

## Содержание документа

**§0. О документе.** Источник `docs/api_17_09_26.json` (107 765 Б, SHA-256 `50f8ed29f9fcf425ab0c0075751a64a474ffc5758f63d06a83f51f7a30fc6b11`), база сравнения `docs/SOM1.json` (69 353 Б, SHA-256 `05dd6e2fcb5178b539bdecaef3c4a44d6d31089acc1a3672188f7e703fbdcd06`); связь с док 01/02/05/08 и `SOM1. Комментарии.md`; правило: документ фиксирует факт изменений, реализация — по плану §8.

**§1. Сводка.** 27/32/34/6 → **34/40/49/10**; удалённых операций и схем нет; `openapi 3.1.0`, `info Energomera 0.1.0`, отсутствие `servers`/`security` — без изменений.

**§2. Новые операции (8, тег `ML models: AutoML`)** — с точными параметрами, телами, кодами ответов и предлагаемыми классами безопасности: создание популяции (`model_id` path + тело `FindParamsPopulationCreateRequest`: `name`, `description`, `population_config`; 201) — `heavy`; `selection` (`population_id` + тело: `train_dataset_id`, `test_dataset_id`, `train_config`, `test_config`, `selection_config`, `name`; **202** → задача `find-params`) — `heavy`; каскадный `DELETE` — `destructive`; `GET` популяции/списка/`mutated_models` (`is_show_best/normal/discarded/failed`, `descending`)/`selections`/`grid.csv` (`selection_id`) — `read`.

**§3. Новые схемы (15)** — назначение каждой: `PopulationConfig`, `MutationRangeRule`/`MutationValuesRule` (discriminator `type`), `MutationMode`, `RoundMode`, `SelectionConfig`, `SortKey`, `FindParamsPopulation`, `FindParamsPopulationCreateRequest/Response`, `FindParamsSelection`, `FindParamsSelectionRunRequest/Response`, `FindParamsMutatedModel`, `FindParamsLabel`.

**§4. Переименование тегов ML models** — 11 операций, только тег: `initialization` (7), `training` (1), `testing` (1), `inference` (2); следствие: модулей 10, подписи длинные, `spec.module` буквально уходит в колонку «Модуль» док 05, в селектор консоли и в `notes.MODULES`.

**§5. Loads: закрытие P0 и удаление `ph_n`** — `phase_connection` удалён из `LoadDevice` (был в `required`) и `UpdateLoadRequest`; `GET /api/loads/list` — 5 фильтров; что это значит для `TC-LOAD-02/03` (переформулировка, снятие `blocked_by_api` и `DEFECT_BY_CHECK`, сохранение истории дефекта как «исправлено контрактом 17.09.2026»).

**§6. `TaskType` += `find-params`** — фильтр «Тип задачи» монитора, перечни в заданиях проверок.

**§7. Оставшиеся расхождения/находки** — отсутствующий `POST /api/ml_models/find_params/population` (упомянут в описании, но не в `paths`); `text/csv` у `grid.csv` (генератор пока отдаёт `json`); пустая схема ответа `loads`; свободные `object` (`config_mutations`, `check_results`, `base_config`, `train_config`); нет `servers`/`security`.

**§8. План работ с очередностью** — К0 (контракт → генератор → реестр 40 → клиент `LoadDevice`/`UpdateLoadRequest`/`TaskType` → `TC-LOAD-02/03` → документы → тесты), К1 (**AutoML полностью и независимо**: схемы + `client/find_params.py` + помощники консоли + группа `TC-AUTO`), К2 (живой прогон на `__TEST__`-популяции + отчёт этапа). Плюс правило порядка: сначала коммит незавершённого T3, затем К0, К1, К2 отдельными коммитами.

**§9. Открытые вопросы** — имя контракта в репозитории и архивная копия 31.08; правило `tools/sync_docs.py` (сейчас `EXTRA_FILES` ждёт имя `SOM1.json`); нужен ли этап под AutoML в ТЗ; как отражать закрытый P0 в отчёте.

**§10. Приложение (без сокращений)**
- **10.1. Полный список 40 операций** — таблица: №, модуль, метод и путь, класс безопасности, тело, вид ответа, параметры (полные имена) — baseline из `python -m tools.gen_endpoints --report --spec docs/api_17_09_26.json`, уже снят (например, №13 `Loads GET /api/loads/list` — `category, load_id, description_search, limit, offset`; №27–34 — AutoML; №35–40 — Task service).
- **10.2. Требования к независимости AutoML (обязательные):**
  1. отдельный файл сценариев `acceptance/checks/automl.py` + отдельный `GroupPlan TC-AUTO`; отсутствие AutoML-эндпоинтов в сборке (404/405/нет пути) → статус «недоступно в сборке»/`SKIPPED`, а не «отказ», и не меняет статусы и агрегаты чужих групп;
  2. AutoML-проверки **не входят** в групповой `tech`-прогон остальных групп и в снимок стенда по умолчанию — запускаются только явным выбором группы;
  3. отсутствие данных (`model_id`/заполненный датасет) → «пропущена (нет данных)», остальные группы не затронуты;
  4. в отчёте — отдельная строка группы, включается только если проверки выполнялись; агрегаты остальных групп считаются без неё;
  5. регрессия: отдельный `tests/unit/test_checks_automl.py`; фейковый стенд без AutoML-маршрутов не ломает `test_checks_loads/files/records/system/tasks` и `pytest -q` целиком (текущий базис — 477 passed, 3 skipped);
  6. идентификаторы `TC-AUTO-01…08` объявляются планом и добавляются в `docs/02` на шаге К1 (в док 09 — как план, чтобы не расходиться с чек-листом).
- **10.3. Инвентарь тестов-сторожей**, которые упадут при замене контракта без правок (с указанием файла и утверждения), — уже пройден: `test_endpoints` (32→40, 6→10), `test_request_complexity_doc` (32→40, шкала S1–S4), `test_api_notes` (шаблоны), `test_checks_catalog` (блокированные проверки), `test_checks_loads` (`FakeLoads` с `ph_n`), `test_notes`/`test_report`/`test_session` (тексты `phase_connection`).

Всё это я готов записать прямо сейчас — но файлы создавать в plan mode нельзя. **Переключите, пожалуйста, на Act mode (toggle to Act mode)** — и я в одну итерацию создам `docs/09. Контракт API 17.09.2026 — изменения и план работ.md` со всеми разделами и приложением §10 и добавлю строку в таблицу `docs/README.md`, после чего проверю результат (регистрация в README, отсутствие правок кода: `git status` покажет только два файла).