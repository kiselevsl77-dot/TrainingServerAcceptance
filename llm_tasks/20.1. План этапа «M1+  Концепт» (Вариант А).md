Принят **Вариант А**. Ниже — финальный план (файлы не изменялись: я в режиме Plan).

---

# План этапа «M1+ / Концепт» (Вариант А)

## Шаг 1. Новый документ концепта

**Создать:** `d:\VSC Python projects\TrainingServerAcceptance\docs\14. Концепт пульта — целевой бизнес-процесс испытаний.md`

Шапка: дата 21.09.2026, основание — замечания заказчика пп. 1–4, статус — **концепт** (описывает целевое состояние, а не текущий код), источники: `docs/01`, `docs/02`, `docs/12` (§3.4, §13, §27), `docs/13`, код `acceptance/**`.

| §    | Заголовок                          | Состав                                                       |
| ---- | ---------------------------------- | ------------------------------------------------------------ |
| 1    | Принцип проведения испытаний       | таблица «5 слоёв» (программа-методика → программа проверок → программа испытаний → обмен → результат), цикл оператора (очередь → «Пуск» → запрос/ответ → вердикт → замечание → следующий тест) |
| 2    | Где маршрут оператора разрывается  | таблица дублирования «Чек-лист» ↔ «Монитор обмена» (список, точка запуска, состояние, KPI, прозрачность) + факты: `session.checks` против `session.plan.items`, лента только в `monitor.py`, расхождение докстринга `exchange_monitor.py:13–14` |
| 3    | Целевое разделение экранов         | таблица трёх ответственностей (Прогон / Протокол / Архив) + 4 принципа (одна очередь, один пуск, один след, «идеальный процесс не знает о дефектах API») |
| 4    | Порядок навигации                  | таблица as-is → to-be с обоснованием (см. Шаг 3)             |
| 5    | Бизнес-процесс (идеальный вариант) | таблицы: 5.1 участники, 5.2 фазы и гейты, 5.3 классы проверок, 5.4 трассируемость, 5.5 жизненный цикл проверки, 5.6 артефакты, 5.7 «вопрос оператора → экран»; **mermaid M1–M7** (контекст, макропроцесс с петлёй регрессии, sequenceDiagram одного «Пуска», stateDiagram проверки, целевое рабочее место, контур замечаний, трассируемость) |
| 6    | План перехода (M2/M3)              | что даёт единый экран «Прогон» (M2) и полная переработка ИА (M3); что не меняется: каталог проверок, отчёт, сессия, правила `__TEST__` |
| 7    | Как удерживается решение           | какие стоп-тесты фиксируют порядок меню и инварианты (Шаг 5) |

Содержимое таблиц и диаграмм — ровно то, что я привёл в предыдущем ответе (§4.1–4.6).

## Шаг 2. Регистрация документа
* `docs\README.md` — строка в таблице документов после `docs/13`: `14. Концепт пульта — целевой бизнес-процесс испытаний.md` с описанием «целевое разделение экранов, порядок навигации, бизнес-процесс приёмки сервера обучения (таблицы + 7 диаграмм mermaid)».
* `README.md` (раздел «Документация испытаний», п. 21–22) — добавить пункт `docs/14` в перечень. Заодно корректно перечислить `docs/12`, `docs/13` (сейчас в перечне только `docs/01…docs/11`).
* Проверено: `tools\sync_docs.py` копирует `*.md` **по именам из каталога постановки**, поэтому `docs/14` (как и `docs/12`, `docs/13`) синхронизация не перезапишет. Правки `docs/01`/`docs/02` в этом варианте **не делаются** → запись в `BACKPORT.md` не требуется.

## Шаг 3. Меню — `acceptance\app.py` (строки 61–65)

```python
# Порядок групп повторяет бизнес-процесс испытаний (docs/14):
# подготовка → прогон → данные стенда → инфраструктура → инструменты → результаты.
GROUPS: list[tuple[str, list[str]]] = [
    ("Подготовка", ["stand", "session"]),
    ("Испытания", ["checks", "monitor", "records", "datasets", "tasks"]),
    ("Инструменты", ["console"]),
    ("Результаты", ["notes", "logs", "report"]),
]
```

Обоснование (в комментарии кода и в §4 документа):
* первая группа получает заголовок «Подготовка» — сейчас она без заголовка и выглядит «подвешенной»;
* `$Задачи` — последним в «Испытаниях» (ваше замечание): это монитор инфраструктуры, он обслуживает прогон, а не шаг приёмки; `checks`/`monitor` (прогон и наблюдение) — первыми, данные стенда — между ними; место будущих `TC-MOD`/`TC-TR`/`TC-INF` — после `datasets`;
* `console` вынесена в «Инструменты»: в маршруте оператора консоль — диагностика/аварийный доступ (FR-T3), впереди остальных разделов она сбивает с шага 6 сценария §7.3 руководства.

## Шаг 4. Синхронизация документации по меню
* `docs\12` **§3.4** — mermaid «Карта разделов и маршрут оператора»: новые группы/порядок.
* `docs\12` **§7.3** (порядок работы оператора) — шаги «обзорные экраны»/«консоль» привести в соответствие (задачи — последними, консоль — инструмент вне маршрута).
* `docs\12` **§26.3** (карта «раздел ↔ требование») — порядок строк по новым группам; **§26.5** — добавить ссылку на `docs/14`; **§26.7** — внести `docs/14` (заголовки `### 26.7.`/`### 26.8.` не менять — они якоря стоп-теста); **§26.8** — строка истории `1.5 | 21.09.2026 | Концепт и порядок навигации`.

## Шаг 5. Тест-«замок» на порядок меню (рекомендую, но это опционально)

Порядок меню сейчас **не закреплён** тестами (смоук-тест `test_every_registered_screen_renders` перечисляет экраны своей константой, порядок ему безразличен) — значит следующая правка молча его вернёт. Варианты:

| Вариант            | Как                                                          | Плюсы/минусы                                                 |
| ------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **A (рекомендую)** | вынести чистые данные навигации в новый `acceptance/ui/nav.py` (`GROUPS`, ключи экранов) без импорта Streamlit; `app.py` импортирует их; новый `tests/unit/test_nav.py`: `$Задачи` — последняя в «Испытаниях», `console` — в «Инструментах», состав групп = состав `SCREENS` | тест проверяет данные, а не текст; поведение и разметка экранов не меняются |
| B                  | без нового модуля: unit-тест читает исходник `app.py` регуляркой | нулевой рефакторинг, но хрупко к форматированию              |
| C                  | отложить тест                                                | быстрее, но решение не защищено                              |

## Шаг 6. Отметка выполненного
`llm_tasks\20.1. Что сделано.md` — по конвенции репозитория (что создано/изменено, результаты гейтов, что осталось на M2/M3).

## Шаг 7. Гейты и ручная проверка
```powershell
python -m pytest                                   # 671 тест (сейчас: 671 collected)
python -m pytest tests/unit/test_user_guide_doc.py tests/unit/test_glossary.py tests/smoke/test_app_smoke.py
python -m ruff check . ; python -m ruff format --check .
python -m mypy client lib acceptance tests
python -m tools.gen_endpoints --check              # реестр не затрагивается, но прогон дешёвый
```
Ручная проверка: `run_pult.bat` → боковая панель = **Подготовка** (`🖥️ Стенд`, `🧪 Сессия испытаний`) → **Испытания** (`✅ Чек-лист проверок`, `🔄 Монитор обмена`, `🧩 Записи`, `🗂️ $Датасеты`, `⏱️ $Задачи` последним) → **Инструменты** (`📡 Консоль запросов`) → **Результаты** (`✍️ Замечания к API`, `🧾 Журнал`, `📄 Отчёт испытаний`).

## Риски (низкие) и что осознанно не делаем
* **Порядок меню** в `groups` не влияет на маршрутизацию (`state.session_state["pult_screen"]`), ссылки «открыть в консоли/журнале» из других экранов остаются рабочими.
* **Стоп-тесты документации** проверяют только §15.5/§25.2, шапку, §26.7 и числа каталога — правки §3.4/§26.3/§26.5/§26.7/§26.8 безопасны, если заголовки-якоря не переименовывать.
* Осознанно **не делаем** в этом этапе: объединение «Чек-лист» + «Монитор обмена» (M2), снятие отдельной сущности «план», переработку разделов отчёта и схемы сессии (M3), устранение двойного учёта `session.plan.items` — всё это описано в §6 концепта как следующие шаги.

---

Готов выполнить шаги 1–4 и 6–7 сразу после переключения в **Act mode**; по шагу 5 скажите, вариант A/B/C (мой выбор по умолчанию — **A**, если промолчите, сделаю A).