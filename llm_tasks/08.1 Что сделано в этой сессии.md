Этап T3 завершён. Продолжение работы (с прошлого сообщения) и итог:

## Что сделано в этой сессии

**1. Тесты сценариев — дописаны и зелёные**
- `tests/unit/test_checks_records.py` — добавлены `TC-REC-05/06` (разбор разметки, лимит `markup_files`, «блокировано API» когда ничего не скачалось, сравнение версий одной записи); 13 тестов.
- `tests/unit/test_checks_loads.py` (новый, 17 тестов) — реестр и срез `limit`/`offset`, отсутствие `phase_connection` → BLOCKED + автозамечание P0, `ph_n` игнорируется → BLOCKED, точные/регистрозависимые фильтры, поиск по описанию, категории (`__OTHER__`, регистровые дубли), проба `DELETE /api/loads/{id}` 404/405 и «удалил» → отказ.
- `tests/unit/test_report.py` (новый, 26 тестов) — все 11 разделов, «отчёт не ходит в сеть» (подменён `httpx.Client.send`), предупреждения о полноте, сравнение снимков с дельтой, авто-замечание P0 для `TC-LOAD-02`, задачи/FSM-1, приложения CSV/JSONL, сохранение комплекта, приложение «манифест записей».
- `tests/integration/test_live_write.py` (новый) — живые `TC-FILE-11/12/13` под `PULT_LIVE_WRITE=1` с проверкой самоочистки (в реестре не осталось `__TEST__…` этой сессии, в учёте — пары «создан/удалён») и отдельный прогон технического чек-листа.
- Точечные дополнения: `test_notes.py` (`defect_for`, `note_from_check`), `test_session.py` (учёт `__TEST__`-сущностей, схема v4), smoke (фильтры чек-листа, экран отчёта и сохранение комплекта, фильтр журнала по метке проверки).

**2. Дефекты, найденные тестами и исправленные**
- `checks/records.py`: обращение к несуществующему `record.delta_label` → `record.delta_note` (падала `TC-REC-01`).
- `checks/loads.py`: пустой реестр в `TC-LOAD-01` давал «отказ» → теперь `PreconditionError` («пропущена»).
- `report.py::_tasks_section`: цепочка FSM-1 читала несуществующие ключи `from`/`to` (печаталось `→ None`) — теперь `previous → status` + примечание; `polls` берутся из `poll.polls`.
- `report.py`: добавлено приложение **«манифест записей»** (`manifest_annexes`, `MANIFEST_KIND_PREFIX`) — файлы `records_manifest_*`, выгруженные экраном «Записи»; нечитаемый файл даёт предупреждение о полноте.
- `notes.py`: `sorted_notes`/`notes_by_priority`/`has_note` принимают `Sequence` (иначе mypy падал на `session.notes`).
- `checks/tasks.py`: 11 обращений `context.tasks.…` → `context.tasks_api().…` (после того как `AutomationContext.tasks` стал `TasksApi | None`, mypy-гейт падал).
- `tests/unit/test_task_snapshot.py`: устранена зависимость от календарной даты (тест падал на реальных датах, т.к. `archive_summary` сравнивает с `datetime.now()`).

**3. Изменение поведения интерфейса (осознанное)**
KPI экрана «Чек-лист проверок» теперь считаются **по видимым строкам** (все активные фильтры): раньше «В выборке/Выполнено/…» зависели только от группы, и при фильтре по классу или поиску KPI расходился с таблицей (например, KPI 41 при 6 строках). Добавлен `_rows_stats`.

**4. Живой прогон (стенд `dev@58ac72f`, 17.09.2026)**
- успех: `TC-SYS-01/02/04/05`, `TC-FILE-01…09/14`, `TC-REC-01/02/04/05/06`, `TC-LOAD-01/04/05/06`;
- «блокировано API»: `TC-FILE-10`, `TC-LOAD-02`, `TC-LOAD-03` (движок сам создаёт замечания);
- **отказ `TC-SYS-03`**: `/api/__test_unknown__` отвечает **405 вместо 404** — живое подтверждение замечания «`DELETE /api/{file_id}` в корне `/api/` (риск коллизий)».

**5. Документы и гейты**
- `docs/08. Этап T3 — чек-лист и отчёт.md`, обновлены `README.md` (экраны, структура, события журнала, тесты, `PULT_LIVE_WRITE`, таблица этапов: T3 — выполнен) и `docs/README.md`.
- `pytest` — **477 passed, 3 skipped**; `pytest -m integration` — 16 passed, 3 skipped; `ruff check` — чисто; `ruff format --check` — 131 файл; `mypy acceptance client lib tools` — без замечаний; `python -m tools.gen_endpoints --check` — реестр актуален (32 операции).

Осталось на будущие этапы (зафиксировано в `docs/08`, §5): `TC-SYS-03` останется красным, пока сервер не исправит обработку неизвестного пути; `TC-LOAD-07` — деструктивная проба, запускается только вручную/под флагом; полный комплект отчёта (сравнение сессий, финализация с подписями) — T9.