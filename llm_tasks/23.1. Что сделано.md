Работа по этому шагу завершена. Итог — ниже.

## Что сделано

**1. Правила работы — `.clinerules` (37 строк)**
Зафиксированы правила диалога (вопросы только таблицей со свободным столбцом «Ответ», никаких интерактивных виджетов), plan-режим только для чтения, архитектурные решения big-bang, список переиспользуемых слоёв и состав гейтов.

**2. Утверждённый план — `llm_tasks/23. Утверждённый план «Big bang — ядро и 15 экранов».md` (293 строки)**
Разделы: решения Q1–Q10 + принятые по умолчанию Q11–Q16; карта «переиспользуем / переписываем / удаляем»; ревизия архитектурного разреза (плоские модули `acceptance/*.py` вместо пакета `core/` — с обоснованием); схема сессии v7 и файла `check_sets.json`; API модулей; этапы 0–6 с гейтами; тестовая матрица; риски; **журнал решений** и **состояние работ** с фактическими результатами гейтов.

**3. Ядро планирования — этап 1a (код + 46 тестов)**

| Файл                           | Объём                | Содержание                                                   |
| ------------------------------ | -------------------- | ------------------------------------------------------------ |
| `acceptance/sets.py`           | 738 строк            | `CheckSet`/`SetItem`/`SetRevision`/`SetsLibrary`: состав с порядком, раздел и целевой объём (`смоук`/`стандарт`/`полный`), **неизменяемые ревизии**, утверждение (`FR-P-65…67`), файл `check_sets.json`, стартовая библиотека из каталога + `SET-SMOKE` |
| `acceptance/programme.py`      | 897 строк            | `Programme`: **объединение по ИЛИ** с дедупликацией и происхождением, обязательные пункты первыми, покрытие разделов и предупреждения, обоснование сокращения, ревизии/утверждение, шаблоны `Полная/Смоук/Регресс`, хранение в сессии (`FR-P-68…70`) |
| `acceptance/session.py`        | правка               | схема **v7**: добавлены `programme`, `queue`, `check_history`; `plan` помечен legacy до этапа 2 |
| `tests/unit/test_sets.py`      | 337 строк, 19 тестов | состав/порядок/обязательность, ревизии и откат, копирование, файл, битые данные |
| `tests/unit/test_programme.py` | 409 строк, 27 тестов | объединение и происхождение, покрытие, предупреждения, ревизии/утверждение/сокращение, шаблоны, `rows` из результатов, сессия |
| `tests/unit/test_session.py`   | правка               | тест схемы обновлён на v7                                    |

**4. Найденные и исправленные при тестировании дефекты модели** (занесены в журнал решений плана):
- правка состава, номер ревизии которой уже в архиве, теперь поднимает номер — архив не «делит» номер с изменённым составом;
- `archive()` больше не перезаписывает комментарий зафиксированной ревизии (иначе `revise` затирал историю);
- `delta(номер)` при отсутствующей ревизии возвращает `missing: True`, а не молчаливую дельту с ближайшей.

## Гейты (фактические)

```
python -m pytest -q                     → 744 passed, 3 skipped   (было 698)
python -m ruff check .                  → All checks passed
python -m ruff format --check .         → 199 files already formatted
python -m mypy acceptance tests         → Success: no issues found in 110 source files
python -m tools.gen_endpoints --check   → актуально: 40 операций
node --check mockup/{app,demo-data}.js  → ok
```

Дополнительно прогнан ручной вертикальный срез ядра: «библиотека наборов → программа из трёх наборов с пересечением (2 проверки учтены один раз, происхождение «SET-SYS (рев. 1), SET-SMOKE (рев. 1)») → покрытие 100 % → утверждение → правка заблокирована → новая ревизия → исключение обязательного пункта отклонено (`FR-P-70`) → исключение пункта даёт непокрытый раздел → утверждение требует обоснования (`FR-P-69`) → дельта ревизии → шаблоны → сохранение в сессию». Временные файлы смоука удалены.

## Что осталось (следующий шаг — этап 1b)

1. `acceptance/queue.py` — очередь как **снимок утверждённой ревизии**: маркеры «следующая/выполняется/последняя», «снято с причиной» = результат, запрет правки состава (норма `IR-P-18`) + `tests/unit/test_queue.py` (~12 тестов).
2. `acceptance/results.py` — единый след результата `DR-P-5` и история повторов (`check_history`) + тесты.
3. `acceptance/runner.py` — «▶ Следующая: `TC-…`», пачка `tech`, авто с паузой, подтверждение `live`/`heavy` + тесты (перенос полезных кейсов из `test_plan_runner.py`).
4. Затем этап 2 — новый каркас интерфейса (пять групп, `SCR-01…SCR-15`) и единовременное удаление старого планировщика и `ui/pages/**`.

Решения по умолчанию Q11–Q16 приняты и зафиксированы в плане; если по какому-то из них вы хотите другое значение — достаточно назвать номер, переиграю до начала этапа 2 (пока это ничего не ломает).