Не завис — и работа уже полностью завершена. Переключение в plan-режим остановило меня за шаг до `git push`; сейчас всё дописано, проверено и отправлено в GitHub.

## Фактическое состояние

| Проверка                 | Результат                                                    |
| ------------------------ | ------------------------------------------------------------ |
| `git status --porcelain` | пусто (дерево чистое)                                        |
| `git status -sb`         | `## master...origin/master` (синхронно, ничего не «висит»)   |
| Коммиты                  | `cdd0796` — «этап 1b: ядро прогона — results.py, queue.py, runner.py»; `96b4254` — правка формулировки отчёта |
| Ветки                    | `master` = разработка big bang, `origin/master` совпадает; `stable` и тег `pre-bigbang` не тронуты |

## Что сделано в этапе 1b

| Артефакт                | Объём    | Суть                                                         |
| ----------------------- | -------- | ------------------------------------------------------------ |
| `acceptance/results.py` | 431      | единый след результата (`DR-P-5`), история повторов (`FR-P-36`), снятие с причиной и возврат как результат (`FR-P-15`) |
| `acceptance/queue.py`   | 528      | очередь — снимок ревизии программы (`FR-P-13`), маркеры «следующая/выполняется/последняя» (`IR-P-20`), запрет правки состава (`IR-P-18`, тест-«замок») |
| `acceptance/runner.py`  | 1000     | одна команда «▶ Следующая: `TC-…`» (`IR-P-4`), режимы «сценарием»/«по вызовам», «Пачка tech», авто-прогон, карточка запуска для `live`/`heavy`, оценка вызова |
| Тесты                   | 55 новых | `test_results` (14), `test_queue` (16), `test_runner` (25)   |

**Гейты (факт):** `pytest` → 799 passed / 3 skipped; `ruff check` → чисто; `ruff format --check` → 0 файлов требуют правки; `mypy acceptance tests` → 116 файлов без замечаний; `gen_endpoints --check` → 40 операций; `node --check` → ok.

**Ручной вертикальный срез пройден** на подменённом стенде: 7 наборов → программа из 3 наборов (16 пунктов, 2 пересечения) → утверждение → очередь (состав = программе) → «Следующая» **настоящим сценарием** `TC-SYS-01` (успех, журнал #1–#1, маркер «последняя») → повтор (история = `['успех']`) → снятие с причиной (программа не изменилась) → возврат → «Пачка tech» (отказ не прервал: после пропусков и отказа `TC-TASK-02` — успех) → авто остановилось на ручном пункте → «по вызовам» (журнал +1, результатов проверки +0, `A2`) → сессия сохранена и восстановлена (режим «по вызовам», 5 результатов, история повторов 1). Полный вывод — в `llm_tasks/23.2. Что сделано.md`.

## Что нужно знать

* **Схема сессии осталась v7** (новые поля — внутри `session.queue`), миграция не нужна; `test_session.py` не менялся.
* Старые `plan.py`, `plan_runner.py`, `ui/**` **не тронуты** — удаление на этапе 2 (как утверждено).
* Два замечания вынесены в отчёт: инцидент с кодировкой (PowerShell испортил 2 UTF-8-файла — пересозданы, проверено; текст в репозитории правлю только редактором) и переименование `llm_tasks/23.2 Что сделано.md` → `llm_tasks/23.2. Вопросы и ответы (этап 1b).md` (там были ваши ответы «ОК» на вопросы 1–5).
* **4 уточнения на ваше подтверждение** (в отчёте, §4): карточка запуска обязательна и в режиме «по вызовам»; шаг «по вызовам» без path-параметра/корректного JSON не отправляется и не «съедает» шаг; «Пачка tech» останавливается на первом не-`tech` пункте (в прототипе перепрыгивала); параметры запуска вызова хранятся в пункте очереди.

Дальше по плану — **этап 2**: новый каркас экранов и удаление старого UI/`plan*.py`. Если хотите, оформлю вопросы таблицей (в том числе по этим 4 уточнениям) — скажите, и перейду к этапу 2.