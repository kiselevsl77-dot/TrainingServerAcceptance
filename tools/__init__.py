"""Инженерные скрипты пульта испытаний (не входят в поставку приложения).

sync_vendor.ps1   — вендоринг `client/`/`lib/` из основного UI;
sync_docs.py      — копирование документов постановки в `docs/`;
gen_endpoints.py  — сборка реестра эндпоинтов консоли из `docs/SOM1.json`;
probe_records.py  — живая проверка объединения файлов в записи.
"""
