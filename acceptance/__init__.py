"""Пульт испытаний сервера обучения (временный UI для приёмки текущего API).

Пакет содержит только код, относящийся к испытаниям:
    * `paths.py`     — каталоги данных (сессии, журналы, отчёты, артефакты);
    * `config.py`    — настройки пульта из переменных окружения;
    * `logging_setup.py` — конфигурация loguru (консоль, файл, JSONL сессии);
    * `http_log.py`  — логирующий транспорт httpx и журнал обмена с сервером;
    * `session.py`   — модель сессии испытаний и её персистентность;
    * `notes.py`     — реестр замечаний к API;
    * `api.py`       — сборка клиента API и агрегата `Apis`;
    * `checks/`      — реестр проверок чек-листа (этап T3);
    * `ui/`          — экраны пульта.

Копии `client/` и `lib/` из проекта TrainingServerUI — замороженный вендорный код,
см. `VENDOR.md`.
"""
