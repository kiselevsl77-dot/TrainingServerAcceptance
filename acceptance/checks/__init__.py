"""Чек-лист испытаний: модели, каталог проверок и исполнители сценариев.

    registry.py — модели описания проверки и её результата (T0);
    catalog.py  — каталог проверок по группам `TC-*` (с T4 — группа `TC-TASK`);
    engine.py   — движок проверки: метка `TC-…`, вердикт, диапазон журнала, запись в сессию (T4);
    tasks.py    — автоматические сценарии проверок задач `TC-TASK-01…08` (T4).

Группы T3+ (`TC-SYS`, `TC-FILE`, `TC-REC`, `TC-LOAD`, затем `TC-DS`, `TC-MOD`,
`TC-TR`, `TC-INF`, `TC-CLEAN`) добавляются в каталог без изменения движка.
"""
