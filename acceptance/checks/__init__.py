"""Чек-лист испытаний: модели, каталог проверок и исполнители сценариев.

Этап T0 содержит только модели (`registry.py`). На этапе T3 добавляется каталог
проверок (`catalog.py`) и исполнители (`runner.py`), на этапе T4 — проверки задач
(`tasks.py`), далее — `datasets.py`, `models.py`, `training.py`, `inference.py`.
"""
