"""Компоненты интерфейса пульта: то, что повторяется на разных экранах.

Этап 2 вводит только каркас: `screen.render_screen` рисует трёхзонную раскладку макета
(`docs/16` §0) — рабочая область и панель контекста — и содержимое скелета экрана
(назначение, блоки, состояния, переходы, требования). Наполнение компонентов
(очередь, лента обмена, карточка запуска, KPI, «Внимание», справка «?») — этап 3.
"""

from __future__ import annotations

from acceptance.ui.components.screen import ScreenContent, render_screen

__all__ = ["ScreenContent", "render_screen"]
