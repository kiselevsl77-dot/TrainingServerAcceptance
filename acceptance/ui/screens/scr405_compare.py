"""`SCR-405` Сравнение сессий — Результаты (Ф3–Ф4). Скелет каркаса: наполнение — этап 3.

Макет экрана — `docs/16`, раздел «SCR-405. Сравнение сессий»; требования ТЗ: `FR-P-42`, `FR-P-43`.
Содержимое модуля (назначение, блоки, состояния, переходы) перенесено из макета: оно служит
планом наполнения экрана на этапе 3 и трассировкой «экран ↔ требование» на этапе 4.
"""

from __future__ import annotations

from acceptance.ui.components.screen import ScreenContent, render_screen

#: Ключ маршрута экрана (`acceptance/ui/nav.py`).
KEY = "scr405_compare"

#: Содержимое скелета: что известно об экране из макета до реализации.
CONTENT = ScreenContent(
    purpose="Сравнить две сессии или сборки: дельта проверок, замечаний и реестров.",
    blocks=(
        "Выбор сессий: что с чем сравниваем, фильтр «изменившиеся»",
        "Сводка проверок: сколько изменилось и как",
        "Дельта замечаний: закрытые, открытые, новые",
        "Дельта реестров по снимкам состояния",
        "Регресс: сбор подмножества для перепрогона (`FR-P-41`)",
    ),
    states=(
        "Одна сессия: «Выберите вторую сессию для сравнения»",
        "Сборки совпадают: сравнение показывает только изменение данных",
    ),
    transitions=(
        ("SCR-401", "Протокол проверок"),
        ("SCR-403", "Замечания к API"),
        ("SCR-102", "Программа сессии"),
    ),
    requirements=(
        "FR-P-42",
        "FR-P-43",
    ),
)


def render() -> None:
    """Рисует скелет экрана в общей трёхзонной раскладке (наполнение — этап 3)."""
    render_screen(KEY, CONTENT)
