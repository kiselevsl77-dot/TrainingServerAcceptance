"""`SCR-303` $Задачи — Испытания (Ф1–Ф2). Скелет каркаса: наполнение — этап 3.

Макет экрана — `docs/16`, раздел «SCR-303. $Задачи»; требования ТЗ: `FR-P-24…FR-P-26`, `DR-P-10`, `IR-P-2`.
Содержимое модуля (назначение, блоки, состояния, переходы) перенесено из макета: оно служит
планом наполнения экрана на этапе 3 и трассировкой «экран ↔ требование» на этапе 4.
"""

from __future__ import annotations

from acceptance.ui.components.screen import ScreenContent, render_screen

#: Ключ маршрута экрана (`acceptance/ui/nav.py`).
KEY = "scr303_tasks"

#: Содержимое скелета: что известно об экране из макета до реализации.
CONTENT = ScreenContent(
    purpose="Наблюдать за $Задачами стенда: список и архив, команды FSM-1, история запусков.",
    blocks=(
        "Наблюдение за активными $Задачами и их статусами",
        "Список и архив $Задач с фильтрами и пагинацией",
        "Команды FSM-1 с подтверждением",
        "История запусков и карточка $задачи",
    ),
    states=(
        "Пусто: активных $Задач нет",
        "Нет карточки: статус показан как «нет данных», а не выдуман",
        "Ошибка связи: причина и номер попытки",
    ),
    transitions=(
        ("SCR-301", "Прогон"),
        ("SCR-403", "Замечания к API"),
    ),
    requirements=(
        "FR-P-24…FR-P-26",
        "DR-P-10",
        "IR-P-2",
    ),
)


def render() -> None:
    """Рисует скелет экрана в общей трёхзонной раскладке (наполнение — этап 3)."""
    render_screen(KEY, CONTENT)
