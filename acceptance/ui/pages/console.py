"""Экран «Консоль запросов» (FR-T3, этап T2).

Назначение: произвольный запрос к испытуемому серверу (метод, путь, query,
JSON-тело, multipart-файл) с шаблонами по всем эндпоинтам `SOM1.json`, выводом
статуса/заголовков/тела/длительности и возможностью:
    * приложить обмен к проверке чек-листа (задать метку `TC-…`);
    * создать замечание к API прямо из результата (FR-T7).

Любой запрос консоли попадает в журнал: он выполняется тем же
`LoggingTransport`, что и запросы экранов и проверок.
"""

from __future__ import annotations

import streamlit as st


def render() -> None:
    """Отрисовывает заглушку экрана (реализация — этап T2)."""
    st.title("Консоль запросов")
    st.info(
        "Экран будет реализован на этапе **T2**: произвольные запросы к API с шаблонами, "
        "метками проверок, созданием замечаний к API и полным протоколированием."
    )
    st.caption(
        "Предупреждение: изменяющие и ресурсоёмкие запросы выполняются только после "
        "подтверждения оператора (NFR-T4)."
    )
