"""Экран «Отчёт испытаний» (FR-T8/FR-T9, этапы T3/T5).

Назначение: одна кнопка — и готовый комплект отчётности по сессии:
    * отчёт `*.md` (шапка сессии, условия, снимок стенда, сводка и детализация
      проверок, замечания к API по приоритетам, ресурсоёмкие операции, выводы);
    * отчёт `*.json` (машиночитаемая копия для обработки и архива);
    * приложения: манифест объединения записей (CSV), таблица проверок (CSV),
      реестр замечаний (CSV), выдержки из структурного журнала (JSONL).

Отчёт строится **только** по данным сессии и журнала, поэтому воспроизводим:
любой шаг проверки можно повторить по записи `http_request`/`http_response`.
"""

from __future__ import annotations

import streamlit as st


def render() -> None:
    """Отрисовывает заглушку экрана (реализация — этапы T3/T5)."""
    st.title("Отчёт испытаний")
    st.info(
        "Экран будет реализован на этапах **T3** (первая версия отчёта md/json) и "
        "**T5** (полный комплект: приложения, сравнение сессий, финализация с подписями)."
    )
    st.caption(
        "Отчёт формируется по данным сессии и структурного журнала — никакие данные "
        "не запрашиваются у сервера в момент формирования."
    )
