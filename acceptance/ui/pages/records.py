"""Экран «Записи и субдатасеты» (FR-T2, этап T1).

Назначение: прозрачное объединение RAW- и markup-файлов в **записи** при том,
что в API такой сущности нет. Планируется:
    * таблица записей с обеими частями (`id`, `size`, `import_date`, `s3_path`);
    * отдельные разделы для дублей имён, несопоставленных RAW/markup и прочих файлов;
    * ручная перепривязка markup ↔ RAW оператором (решение фиксируется в журнале);
    * разбор разметки (записи/чанки) по доступным markup-файлам;
    * выгрузка **манифеста объединения** (CSV/JSON) для приложения к отчёту.
"""

from __future__ import annotations

import streamlit as st


def render() -> None:
    """Отрисовывает заглушку экрана (реализация — этап T1)."""
    st.title("Записи и субдатасеты")
    st.info(
        "Экран будет реализован на этапе **T1**: записи (пары RAW+markup), дубли имён, "
        "ручные привязки и манифест объединения для отчёта об испытаниях."
    )
    st.caption(
        "В API текущей версии сервера субдатасетов нет — группировка выполняется пультом "
        "и помечается как представление UI (перспективное требование к API)."
    )
