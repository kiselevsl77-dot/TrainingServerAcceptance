"""Экраны пульта испытаний.

Структура экранов (FR-T1…FR-T9):
    stand.py    — «Стенд»: доступность, сборка, снимок реестров (T0);
    session.py  — «Сессия испытаний»: поля, статус, история, файл сессии (T0);
    logs.py     — «Журнал»: обмен с сервером, файлы журналов, выгрузка (T0);
    records.py  — «Записи и субдатасеты»: объединение RAW+markup (T1);
    checks.py   — «Чек-лист проверок»: запуск и результаты (T3/T4);
    console.py  — «Консоль запросов»: произвольный запрос к API (T2);
    report.py   — «Отчёт испытаний»: md/json + приложения (T3/T5).
"""
